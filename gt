#!/usr/bin/env bash
# This script was generated by bashly 1.1.9 (https://bashly.dannyb.co)
# Modifying it manually is not recommended

# :wrapper.bash3_bouncer
if [[ "${BASH_VERSINFO:-0}" -lt 4 ]]; then
  printf "bash version 4 or higher is required\n" >&2
  exit 1
fi

# :command.master_script

# :command.version_command
version_command() {
  echo "$version"
}

# :command.usage
gt_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt - OS Scripts CLI — Install & manage DevOps tools\n"
    echo

  else
    printf "gt - OS Scripts CLI — Install & manage DevOps tools\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt COMMAND\n"
  printf "  gt [COMMAND] --help | -h\n"
  printf "  gt --version | -v\n"
  echo
  # :command.usage_commands
  printf "%s\n" "Commands:"
  printf "  %s   Install tools and components\n" "install    "
  printf "  %s   Uninstall tools and components\n" "uninstall  "
  printf "  %s   Configure installed tools\n" "setup      "
  printf "  %s   Check for outdated tools\n" "outdated   "
  printf "  %s   Generate shell completion script\n" "completions"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo
    printf "  %s\n" "--version, -v"
    printf "    Show version number\n"
    echo

    # :command.usage_environment_variables
    printf "%s\n" "Environment Variables:"

    # :environment_variable.usage
    printf "  %s\n" "GITHUB_TOKEN"
    printf "    GitHub API token (raises rate limit 60→5000 req/hr for \`gt outdated\`)\n"
    echo

    # :environment_variable.usage
    printf "  %s\n" "GT_DOWNLOAD_TIMEOUT"
    printf "    Override curl download timeout in seconds (default 30)\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  gt install kubectl\n"
    printf "  gt install kubectl --version v1.30.0\n"
    printf "  gt install multi kubectl k9s argocd\n"
    printf "  gt outdated --all\n"
    echo

    # :command.footer
    printf "Repo:    https://github.com/ttungbmt/os-scripts\nRoadmap: see ROADMAPS.md\n\n"
    echo

  fi
}

# :command.usage
gt_install_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install - Install tools and components\n"
    echo

  else
    printf "gt install - Install tools and components\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install COMMAND\n"
  printf "  gt install [COMMAND] --help | -h\n"
  echo
  # :command.usage_commands
  printf "%s\n" "Commands:"
  printf "  %s   Install multiple tools at once (comma or space separated)\n" "multi      "
  printf "  %s   Install Kustomize\n" "kustomize  "
  printf "  %s   Install k9s\n" "k9s        "
  printf "  %s   Install kubectl\n" "kubectl    "
  printf "  %s   Install fastfetch\n" "fastfetch  "
  printf "  %s   Install direnv\n" "direnv     "
  printf "  %s   Install btop\n" "btop       "
  printf "  %s   Install zsh\n" "zsh        "
  printf "  %s   Install antidote (Zsh plugin manager)\n" "antidote   "
  printf "  %s   Install thefuck\n" "thefuck    "
  printf "  %s   Install argocd\n" "argocd     "
  printf "  %s   Install velero\n" "velero     "
  printf "  %s   Install kubeconform\n" "kubeconform"
  printf "  %s   Install krew\n" "krew       "
  printf "  %s   Install kubent\n" "kubent     "
  printf "  %s   Install sops\n" "sops       "
  printf "  %s   Install age\n" "age        "
  printf "  %s   Install trivy\n" "trivy      "
  printf "  %s   Install kubens\n" "kubens     "
  printf "  %s   Install kubecolor\n" "kubecolor  "
  printf "  %s   Install popeye\n" "popeye     "
  printf "  %s   Install kube-linter\n" "kube-linter"
  printf "  %s   Install kyverno\n" "kyverno    "
  printf "  %s   Install conftest\n" "conftest   "
  printf "  %s   Install vault\n" "vault      "
  printf "  %s   Install kubeseal\n" "kubeseal   "
  printf "  %s   Install zoxide\n" "zoxide     "
  printf "  %s   Install eza\n" "eza        "
  printf "  %s   Install bat\n" "bat        "
  printf "  %s   Install ripgrep\n" "ripgrep    "
  printf "  %s   Install starship\n" "starship   "
  printf "  %s   Install fzf\n" "fzf        "
  printf "  %s   Install fd (fast find alternative)\n" "fd         "
  printf "  %s   Install mcfly (smart shell history)\n" "mcfly      "
  printf "  %s   Install infisical CLI\n" "infisical  "
  printf "  %s   Install ksops (kustomize-sops KRM plugin)\n" "ksops      "
  printf "  %s   Install skopeo (container image tool)\n" "skopeo     "
  printf "  %s   Install helmfile\n" "helmfile   "
  printf "  %s   Install helm\n" "helm       "
  printf "  %s   Install chezmoi (dotfile manager)\n" "chezmoi    "
  printf "  %s   Install tmux (terminal multiplexer)\n" "tmux       "
  printf "  %s   Install rsync\n" "rsync      "
  printf "  %s   Install trino CLI\n" "trino      "
  printf "  %s   Install nerdctl (containerd CLI)\n" "nerdctl    "
  printf "  %s   Install viddy (modern watch command)\n" "viddy      "
  printf "  %s   Install hyperfine (benchmarking tool)\n" "hyperfine  "
  printf "  %s   Install jq (JSON processor)\n" "jq         "
  printf "  %s   Install yq (YAML processor)\n" "yq         "
  printf "  %s   Install gum (glamorous shell scripts)\n" "gum        "
  printf "  %s   Install uv (Python package manager)\n" "uv         "
  printf "  %s   Install bashly (Bash CLI generator)\n" "bashly     "
  printf "  %s   Install RubyGems (gem)\n" "gem        "
  printf "  %s   Install claude (Claude Code CLI)\n" "claude     "
  printf "  %s   Install talisman (git hook secrets scanner)\n" "talisman   "
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_multi_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install multi - Install multiple tools at once (comma or space separated)\n"
    echo

  else
    printf "gt install multi - Install multiple tools at once (comma or space separated)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install multi TOOLS... [OPTIONS]\n"
  printf "  gt install multi --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if tools are already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "%s\n" "Arguments:"

    # :argument.usage
    printf "  %s\n" "TOOLS..."
    printf "    List of tools to install (e.g., kustomize k9s or kustomize,k9s)\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  gt install multi kubectl k9s argocd\n"
    printf "  gt install multi kubectl,k9s,argocd\n"
    printf "  gt install multi --force kubectl k9s\n"
    echo

  fi
}

# :command.usage
gt_install_kustomize_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install kustomize - Install Kustomize\n"
    echo

  else
    printf "gt install kustomize - Install Kustomize\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install kustomize [OPTIONS]\n"
  printf "  gt install kustomize --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_k9s_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install k9s - Install k9s\n"
    echo

  else
    printf "gt install k9s - Install k9s\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install k9s [OPTIONS]\n"
  printf "  gt install k9s --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_kubectl_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install kubectl - Install kubectl\n"
    echo

  else
    printf "gt install kubectl - Install kubectl\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install kubectl [OPTIONS]\n"
  printf "  gt install kubectl --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_fastfetch_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install fastfetch - Install fastfetch\n"
    echo

  else
    printf "gt install fastfetch - Install fastfetch\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install fastfetch [OPTIONS]\n"
  printf "  gt install fastfetch --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_direnv_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install direnv - Install direnv\n"
    echo

  else
    printf "gt install direnv - Install direnv\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install direnv [OPTIONS]\n"
  printf "  gt install direnv --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_btop_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install btop - Install btop\n"
    echo

  else
    printf "gt install btop - Install btop\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install btop [OPTIONS]\n"
  printf "  gt install btop --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_zsh_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install zsh - Install zsh\n"
    echo

  else
    printf "gt install zsh - Install zsh\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install zsh [OPTIONS]\n"
  printf "  gt install zsh --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_antidote_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install antidote - Install antidote (Zsh plugin manager)\n"
    echo

  else
    printf "gt install antidote - Install antidote (Zsh plugin manager)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install antidote [OPTIONS]\n"
  printf "  gt install antidote --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_thefuck_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install thefuck - Install thefuck\n"
    echo

  else
    printf "gt install thefuck - Install thefuck\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install thefuck [OPTIONS]\n"
  printf "  gt install thefuck --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_argocd_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install argocd - Install argocd\n"
    echo

  else
    printf "gt install argocd - Install argocd\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install argocd [OPTIONS]\n"
  printf "  gt install argocd --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_velero_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install velero - Install velero\n"
    echo

  else
    printf "gt install velero - Install velero\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install velero [OPTIONS]\n"
  printf "  gt install velero --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_kubeconform_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install kubeconform - Install kubeconform\n"
    echo

  else
    printf "gt install kubeconform - Install kubeconform\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install kubeconform [OPTIONS]\n"
  printf "  gt install kubeconform --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_krew_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install krew - Install krew\n"
    echo

  else
    printf "gt install krew - Install krew\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install krew [OPTIONS]\n"
  printf "  gt install krew --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_kubent_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install kubent - Install kubent\n"
    echo

  else
    printf "gt install kubent - Install kubent\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install kubent [OPTIONS]\n"
  printf "  gt install kubent --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_sops_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install sops - Install sops\n"
    echo

  else
    printf "gt install sops - Install sops\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install sops [OPTIONS]\n"
  printf "  gt install sops --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_age_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install age - Install age\n"
    echo

  else
    printf "gt install age - Install age\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install age [OPTIONS]\n"
  printf "  gt install age --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_trivy_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install trivy - Install trivy\n"
    echo

  else
    printf "gt install trivy - Install trivy\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install trivy [OPTIONS]\n"
  printf "  gt install trivy --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_kubens_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install kubens - Install kubens\n"
    echo

  else
    printf "gt install kubens - Install kubens\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install kubens [OPTIONS]\n"
  printf "  gt install kubens --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_kubecolor_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install kubecolor - Install kubecolor\n"
    echo

  else
    printf "gt install kubecolor - Install kubecolor\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install kubecolor [OPTIONS]\n"
  printf "  gt install kubecolor --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_popeye_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install popeye - Install popeye\n"
    echo

  else
    printf "gt install popeye - Install popeye\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install popeye [OPTIONS]\n"
  printf "  gt install popeye --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_kube_linter_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install kube-linter - Install kube-linter\n"
    echo

  else
    printf "gt install kube-linter - Install kube-linter\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install kube-linter [OPTIONS]\n"
  printf "  gt install kube-linter --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_kyverno_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install kyverno - Install kyverno\n"
    echo

  else
    printf "gt install kyverno - Install kyverno\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install kyverno [OPTIONS]\n"
  printf "  gt install kyverno --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_conftest_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install conftest - Install conftest\n"
    echo

  else
    printf "gt install conftest - Install conftest\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install conftest [OPTIONS]\n"
  printf "  gt install conftest --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_vault_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install vault - Install vault\n"
    echo

  else
    printf "gt install vault - Install vault\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install vault [OPTIONS]\n"
  printf "  gt install vault --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_kubeseal_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install kubeseal - Install kubeseal\n"
    echo

  else
    printf "gt install kubeseal - Install kubeseal\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install kubeseal [OPTIONS]\n"
  printf "  gt install kubeseal --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_zoxide_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install zoxide - Install zoxide\n"
    echo

  else
    printf "gt install zoxide - Install zoxide\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install zoxide [OPTIONS]\n"
  printf "  gt install zoxide --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_eza_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install eza - Install eza\n"
    echo

  else
    printf "gt install eza - Install eza\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install eza [OPTIONS]\n"
  printf "  gt install eza --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_bat_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install bat - Install bat\n"
    echo

  else
    printf "gt install bat - Install bat\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install bat [OPTIONS]\n"
  printf "  gt install bat --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_ripgrep_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install ripgrep - Install ripgrep\n"
    echo

  else
    printf "gt install ripgrep - Install ripgrep\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install ripgrep [OPTIONS]\n"
  printf "  gt install ripgrep --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_starship_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install starship - Install starship\n"
    echo

  else
    printf "gt install starship - Install starship\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install starship [OPTIONS]\n"
  printf "  gt install starship --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_fzf_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install fzf - Install fzf\n"
    echo

  else
    printf "gt install fzf - Install fzf\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install fzf [OPTIONS]\n"
  printf "  gt install fzf --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_fd_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install fd - Install fd (fast find alternative)\n"
    echo

  else
    printf "gt install fd - Install fd (fast find alternative)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install fd [OPTIONS]\n"
  printf "  gt install fd --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_mcfly_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install mcfly - Install mcfly (smart shell history)\n"
    echo

  else
    printf "gt install mcfly - Install mcfly (smart shell history)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install mcfly [OPTIONS]\n"
  printf "  gt install mcfly --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_infisical_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install infisical - Install infisical CLI\n"
    echo

  else
    printf "gt install infisical - Install infisical CLI\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install infisical [OPTIONS]\n"
  printf "  gt install infisical --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_ksops_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install ksops - Install ksops (kustomize-sops KRM plugin)\n"
    echo

  else
    printf "gt install ksops - Install ksops (kustomize-sops KRM plugin)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install ksops [OPTIONS]\n"
  printf "  gt install ksops --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_skopeo_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install skopeo - Install skopeo (container image tool)\n"
    echo

  else
    printf "gt install skopeo - Install skopeo (container image tool)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install skopeo [OPTIONS]\n"
  printf "  gt install skopeo --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force reinstall skopeo\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_helmfile_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install helmfile - Install helmfile\n"
    echo

  else
    printf "gt install helmfile - Install helmfile\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install helmfile [OPTIONS]\n"
  printf "  gt install helmfile --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_helm_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install helm - Install helm\n"
    echo

  else
    printf "gt install helm - Install helm\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install helm [OPTIONS]\n"
  printf "  gt install helm --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_chezmoi_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install chezmoi - Install chezmoi (dotfile manager)\n"
    echo

  else
    printf "gt install chezmoi - Install chezmoi (dotfile manager)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install chezmoi [OPTIONS]\n"
  printf "  gt install chezmoi --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_tmux_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install tmux - Install tmux (terminal multiplexer)\n"
    echo

  else
    printf "gt install tmux - Install tmux (terminal multiplexer)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install tmux [OPTIONS]\n"
  printf "  gt install tmux --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force reinstall tmux\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_rsync_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install rsync - Install rsync\n"
    echo

  else
    printf "gt install rsync - Install rsync\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install rsync [OPTIONS]\n"
  printf "  gt install rsync --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force reinstall rsync\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_trino_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install trino - Install trino CLI\n"
    echo

  else
    printf "gt install trino - Install trino CLI\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install trino [OPTIONS]\n"
  printf "  gt install trino --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_nerdctl_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install nerdctl - Install nerdctl (containerd CLI)\n"
    echo

  else
    printf "gt install nerdctl - Install nerdctl (containerd CLI)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install nerdctl [OPTIONS]\n"
  printf "  gt install nerdctl --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_viddy_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install viddy - Install viddy (modern watch command)\n"
    echo

  else
    printf "gt install viddy - Install viddy (modern watch command)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install viddy [OPTIONS]\n"
  printf "  gt install viddy --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_hyperfine_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install hyperfine - Install hyperfine (benchmarking tool)\n"
    echo

  else
    printf "gt install hyperfine - Install hyperfine (benchmarking tool)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install hyperfine [OPTIONS]\n"
  printf "  gt install hyperfine --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_jq_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install jq - Install jq (JSON processor)\n"
    echo

  else
    printf "gt install jq - Install jq (JSON processor)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install jq [OPTIONS]\n"
  printf "  gt install jq --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_yq_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install yq - Install yq (YAML processor)\n"
    echo

  else
    printf "gt install yq - Install yq (YAML processor)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install yq [OPTIONS]\n"
  printf "  gt install yq --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_gum_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install gum - Install gum (glamorous shell scripts)\n"
    echo

  else
    printf "gt install gum - Install gum (glamorous shell scripts)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install gum [OPTIONS]\n"
  printf "  gt install gum --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_uv_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install uv - Install uv (Python package manager)\n"
    echo

  else
    printf "gt install uv - Install uv (Python package manager)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install uv [OPTIONS]\n"
  printf "  gt install uv --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    Version to install (e.g., latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_bashly_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install bashly - Install bashly (Bash CLI generator)\n"
    echo

  else
    printf "gt install bashly - Install bashly (Bash CLI generator)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install bashly [OPTIONS]\n"
  printf "  gt install bashly --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    bashly version to install (e.g., 1.3.8 or latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if bashly is already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_gem_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install gem - Install RubyGems (gem)\n"
    echo

  else
    printf "gt install gem - Install RubyGems (gem)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install gem [OPTIONS]\n"
  printf "  gt install gem --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force reinstall gem\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_claude_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install claude - Install claude (Claude Code CLI)\n"
    echo

  else
    printf "gt install claude - Install claude (Claude Code CLI)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install claude [OPTIONS]\n"
  printf "  gt install claude --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    claude version to install (e.g., 2.1.128 or latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if claude is already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_install_talisman_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt install talisman - Install talisman (git hook secrets scanner)\n"
    echo

  else
    printf "gt install talisman - Install talisman (git hook secrets scanner)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt install talisman [OPTIONS]\n"
  printf "  gt install talisman --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--version, -v VERSION"
    printf "    talisman version to install (e.g., v1.37.0 or latest)\n"
    printf "    Default: latest\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force, -f"
    printf "    Force overwrite if talisman is already installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall - Uninstall tools and components\n"
    echo

  else
    printf "gt uninstall - Uninstall tools and components\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall COMMAND\n"
  printf "  gt uninstall [COMMAND] --help | -h\n"
  echo
  # :command.usage_commands
  printf "%s\n" "Commands:"
  printf "  %s   Uninstall Kustomize\n" "kustomize  "
  printf "  %s   Uninstall k9s\n" "k9s        "
  printf "  %s   Uninstall kubectl\n" "kubectl    "
  printf "  %s   Uninstall fastfetch\n" "fastfetch  "
  printf "  %s   Uninstall direnv\n" "direnv     "
  printf "  %s   Uninstall btop\n" "btop       "
  printf "  %s   Uninstall zsh\n" "zsh        "
  printf "  %s   Uninstall antidote\n" "antidote   "
  printf "  %s   Uninstall thefuck\n" "thefuck    "
  printf "  %s   Uninstall argocd\n" "argocd     "
  printf "  %s   Uninstall velero\n" "velero     "
  printf "  %s   Uninstall kubeconform\n" "kubeconform"
  printf "  %s   Uninstall krew\n" "krew       "
  printf "  %s   Uninstall kubent\n" "kubent     "
  printf "  %s   Uninstall sops\n" "sops       "
  printf "  %s   Uninstall age\n" "age        "
  printf "  %s   Uninstall trivy\n" "trivy      "
  printf "  %s   Uninstall kubens\n" "kubens     "
  printf "  %s   Uninstall kubecolor\n" "kubecolor  "
  printf "  %s   Uninstall popeye\n" "popeye     "
  printf "  %s   Uninstall kube-linter\n" "kube-linter"
  printf "  %s   Uninstall kyverno\n" "kyverno    "
  printf "  %s   Uninstall conftest\n" "conftest   "
  printf "  %s   Uninstall vault\n" "vault      "
  printf "  %s   Uninstall kubeseal\n" "kubeseal   "
  printf "  %s   Uninstall zoxide\n" "zoxide     "
  printf "  %s   Uninstall eza\n" "eza        "
  printf "  %s   Uninstall bat\n" "bat        "
  printf "  %s   Uninstall ripgrep\n" "ripgrep    "
  printf "  %s   Uninstall starship\n" "starship   "
  printf "  %s   Uninstall fzf\n" "fzf        "
  printf "  %s   Uninstall fd\n" "fd         "
  printf "  %s   Uninstall mcfly\n" "mcfly      "
  printf "  %s   Uninstall infisical\n" "infisical  "
  printf "  %s   Uninstall ksops\n" "ksops      "
  printf "  %s   Uninstall skopeo\n" "skopeo     "
  printf "  %s   Uninstall helmfile\n" "helmfile   "
  printf "  %s   Uninstall helm\n" "helm       "
  printf "  %s   Uninstall chezmoi\n" "chezmoi    "
  printf "  %s   Uninstall tmux\n" "tmux       "
  printf "  %s   Uninstall rsync\n" "rsync      "
  printf "  %s   Uninstall trino\n" "trino      "
  printf "  %s   Uninstall nerdctl\n" "nerdctl    "
  printf "  %s   Uninstall viddy\n" "viddy      "
  printf "  %s   Uninstall hyperfine\n" "hyperfine  "
  printf "  %s   Uninstall jq\n" "jq         "
  printf "  %s   Uninstall yq\n" "yq         "
  printf "  %s   Uninstall gum\n" "gum        "
  printf "  %s   Uninstall uv\n" "uv         "
  printf "  %s   Uninstall bashly\n" "bashly     "
  printf "  %s   Uninstall gem\n" "gem        "
  printf "  %s   Uninstall claude\n" "claude     "
  printf "  %s   Uninstall talisman\n" "talisman   "
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_kustomize_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall kustomize - Uninstall Kustomize\n"
    echo

  else
    printf "gt uninstall kustomize - Uninstall Kustomize\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall kustomize\n"
  printf "  gt uninstall kustomize --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_k9s_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall k9s - Uninstall k9s\n"
    echo

  else
    printf "gt uninstall k9s - Uninstall k9s\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall k9s\n"
  printf "  gt uninstall k9s --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_kubectl_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall kubectl - Uninstall kubectl\n"
    echo

  else
    printf "gt uninstall kubectl - Uninstall kubectl\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall kubectl\n"
  printf "  gt uninstall kubectl --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_fastfetch_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall fastfetch - Uninstall fastfetch\n"
    echo

  else
    printf "gt uninstall fastfetch - Uninstall fastfetch\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall fastfetch\n"
  printf "  gt uninstall fastfetch --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_direnv_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall direnv - Uninstall direnv\n"
    echo

  else
    printf "gt uninstall direnv - Uninstall direnv\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall direnv\n"
  printf "  gt uninstall direnv --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_btop_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall btop - Uninstall btop\n"
    echo

  else
    printf "gt uninstall btop - Uninstall btop\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall btop\n"
  printf "  gt uninstall btop --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_zsh_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall zsh - Uninstall zsh\n"
    echo

  else
    printf "gt uninstall zsh - Uninstall zsh\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall zsh\n"
  printf "  gt uninstall zsh --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_antidote_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall antidote - Uninstall antidote\n"
    echo

  else
    printf "gt uninstall antidote - Uninstall antidote\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall antidote\n"
  printf "  gt uninstall antidote --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_thefuck_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall thefuck - Uninstall thefuck\n"
    echo

  else
    printf "gt uninstall thefuck - Uninstall thefuck\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall thefuck\n"
  printf "  gt uninstall thefuck --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_argocd_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall argocd - Uninstall argocd\n"
    echo

  else
    printf "gt uninstall argocd - Uninstall argocd\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall argocd\n"
  printf "  gt uninstall argocd --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_velero_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall velero - Uninstall velero\n"
    echo

  else
    printf "gt uninstall velero - Uninstall velero\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall velero\n"
  printf "  gt uninstall velero --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_kubeconform_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall kubeconform - Uninstall kubeconform\n"
    echo

  else
    printf "gt uninstall kubeconform - Uninstall kubeconform\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall kubeconform\n"
  printf "  gt uninstall kubeconform --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_krew_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall krew - Uninstall krew\n"
    echo

  else
    printf "gt uninstall krew - Uninstall krew\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall krew\n"
  printf "  gt uninstall krew --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_kubent_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall kubent - Uninstall kubent\n"
    echo

  else
    printf "gt uninstall kubent - Uninstall kubent\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall kubent\n"
  printf "  gt uninstall kubent --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_sops_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall sops - Uninstall sops\n"
    echo

  else
    printf "gt uninstall sops - Uninstall sops\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall sops\n"
  printf "  gt uninstall sops --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_age_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall age - Uninstall age\n"
    echo

  else
    printf "gt uninstall age - Uninstall age\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall age\n"
  printf "  gt uninstall age --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_trivy_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall trivy - Uninstall trivy\n"
    echo

  else
    printf "gt uninstall trivy - Uninstall trivy\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall trivy\n"
  printf "  gt uninstall trivy --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_kubens_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall kubens - Uninstall kubens\n"
    echo

  else
    printf "gt uninstall kubens - Uninstall kubens\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall kubens\n"
  printf "  gt uninstall kubens --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_kubecolor_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall kubecolor - Uninstall kubecolor\n"
    echo

  else
    printf "gt uninstall kubecolor - Uninstall kubecolor\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall kubecolor\n"
  printf "  gt uninstall kubecolor --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_popeye_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall popeye - Uninstall popeye\n"
    echo

  else
    printf "gt uninstall popeye - Uninstall popeye\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall popeye\n"
  printf "  gt uninstall popeye --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_kube_linter_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall kube-linter - Uninstall kube-linter\n"
    echo

  else
    printf "gt uninstall kube-linter - Uninstall kube-linter\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall kube-linter\n"
  printf "  gt uninstall kube-linter --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_kyverno_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall kyverno - Uninstall kyverno\n"
    echo

  else
    printf "gt uninstall kyverno - Uninstall kyverno\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall kyverno\n"
  printf "  gt uninstall kyverno --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_conftest_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall conftest - Uninstall conftest\n"
    echo

  else
    printf "gt uninstall conftest - Uninstall conftest\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall conftest\n"
  printf "  gt uninstall conftest --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_vault_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall vault - Uninstall vault\n"
    echo

  else
    printf "gt uninstall vault - Uninstall vault\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall vault\n"
  printf "  gt uninstall vault --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_kubeseal_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall kubeseal - Uninstall kubeseal\n"
    echo

  else
    printf "gt uninstall kubeseal - Uninstall kubeseal\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall kubeseal\n"
  printf "  gt uninstall kubeseal --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_zoxide_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall zoxide - Uninstall zoxide\n"
    echo

  else
    printf "gt uninstall zoxide - Uninstall zoxide\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall zoxide\n"
  printf "  gt uninstall zoxide --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_eza_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall eza - Uninstall eza\n"
    echo

  else
    printf "gt uninstall eza - Uninstall eza\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall eza\n"
  printf "  gt uninstall eza --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_bat_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall bat - Uninstall bat\n"
    echo

  else
    printf "gt uninstall bat - Uninstall bat\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall bat\n"
  printf "  gt uninstall bat --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_ripgrep_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall ripgrep - Uninstall ripgrep\n"
    echo

  else
    printf "gt uninstall ripgrep - Uninstall ripgrep\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall ripgrep\n"
  printf "  gt uninstall ripgrep --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_starship_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall starship - Uninstall starship\n"
    echo

  else
    printf "gt uninstall starship - Uninstall starship\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall starship\n"
  printf "  gt uninstall starship --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_fzf_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall fzf - Uninstall fzf\n"
    echo

  else
    printf "gt uninstall fzf - Uninstall fzf\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall fzf\n"
  printf "  gt uninstall fzf --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_fd_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall fd - Uninstall fd\n"
    echo

  else
    printf "gt uninstall fd - Uninstall fd\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall fd\n"
  printf "  gt uninstall fd --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_mcfly_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall mcfly - Uninstall mcfly\n"
    echo

  else
    printf "gt uninstall mcfly - Uninstall mcfly\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall mcfly\n"
  printf "  gt uninstall mcfly --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_infisical_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall infisical - Uninstall infisical\n"
    echo

  else
    printf "gt uninstall infisical - Uninstall infisical\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall infisical\n"
  printf "  gt uninstall infisical --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_ksops_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall ksops - Uninstall ksops\n"
    echo

  else
    printf "gt uninstall ksops - Uninstall ksops\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall ksops\n"
  printf "  gt uninstall ksops --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_skopeo_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall skopeo - Uninstall skopeo\n"
    echo

  else
    printf "gt uninstall skopeo - Uninstall skopeo\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall skopeo\n"
  printf "  gt uninstall skopeo --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_helmfile_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall helmfile - Uninstall helmfile\n"
    echo

  else
    printf "gt uninstall helmfile - Uninstall helmfile\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall helmfile\n"
  printf "  gt uninstall helmfile --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_helm_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall helm - Uninstall helm\n"
    echo

  else
    printf "gt uninstall helm - Uninstall helm\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall helm\n"
  printf "  gt uninstall helm --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_chezmoi_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall chezmoi - Uninstall chezmoi\n"
    echo

  else
    printf "gt uninstall chezmoi - Uninstall chezmoi\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall chezmoi\n"
  printf "  gt uninstall chezmoi --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_tmux_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall tmux - Uninstall tmux\n"
    echo

  else
    printf "gt uninstall tmux - Uninstall tmux\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall tmux\n"
  printf "  gt uninstall tmux --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_rsync_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall rsync - Uninstall rsync\n"
    echo

  else
    printf "gt uninstall rsync - Uninstall rsync\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall rsync\n"
  printf "  gt uninstall rsync --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_trino_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall trino - Uninstall trino\n"
    echo

  else
    printf "gt uninstall trino - Uninstall trino\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall trino\n"
  printf "  gt uninstall trino --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_nerdctl_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall nerdctl - Uninstall nerdctl\n"
    echo

  else
    printf "gt uninstall nerdctl - Uninstall nerdctl\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall nerdctl\n"
  printf "  gt uninstall nerdctl --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_viddy_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall viddy - Uninstall viddy\n"
    echo

  else
    printf "gt uninstall viddy - Uninstall viddy\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall viddy\n"
  printf "  gt uninstall viddy --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_hyperfine_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall hyperfine - Uninstall hyperfine\n"
    echo

  else
    printf "gt uninstall hyperfine - Uninstall hyperfine\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall hyperfine\n"
  printf "  gt uninstall hyperfine --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_jq_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall jq - Uninstall jq\n"
    echo

  else
    printf "gt uninstall jq - Uninstall jq\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall jq\n"
  printf "  gt uninstall jq --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_yq_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall yq - Uninstall yq\n"
    echo

  else
    printf "gt uninstall yq - Uninstall yq\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall yq\n"
  printf "  gt uninstall yq --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_gum_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall gum - Uninstall gum\n"
    echo

  else
    printf "gt uninstall gum - Uninstall gum\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall gum\n"
  printf "  gt uninstall gum --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_uv_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall uv - Uninstall uv\n"
    echo

  else
    printf "gt uninstall uv - Uninstall uv\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall uv\n"
  printf "  gt uninstall uv --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_bashly_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall bashly - Uninstall bashly\n"
    echo

  else
    printf "gt uninstall bashly - Uninstall bashly\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall bashly\n"
  printf "  gt uninstall bashly --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_gem_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall gem - Uninstall gem\n"
    echo

  else
    printf "gt uninstall gem - Uninstall gem\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall gem\n"
  printf "  gt uninstall gem --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_claude_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall claude - Uninstall claude\n"
    echo

  else
    printf "gt uninstall claude - Uninstall claude\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall claude\n"
  printf "  gt uninstall claude --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_uninstall_talisman_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt uninstall talisman - Uninstall talisman\n"
    echo

  else
    printf "gt uninstall talisman - Uninstall talisman\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt uninstall talisman\n"
  printf "  gt uninstall talisman --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_setup_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt setup - Configure installed tools\n"
    echo

  else
    printf "gt setup - Configure installed tools\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt setup COMMAND\n"
  printf "  gt setup [COMMAND] --help | -h\n"
  echo
  # :command.usage_commands
  printf "%s\n" "Commands:"
  printf "  %s   Configure multiple tools at once (comma or space separated)\n" "multi   "
  printf "  %s   Configure antidote Zsh plugin manager (~/.zshrc and ~/.zsh_plugins.txt)\n" "antidote"
  printf "  %s   Configure starship prompt (~/.config/starship.toml)\n" "starship"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
gt_setup_multi_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt setup multi - Configure multiple tools at once (comma or space separated)\n"
    echo

  else
    printf "gt setup multi - Configure multiple tools at once (comma or space separated)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt setup multi TOOLS... [OPTIONS]\n"
  printf "  gt setup multi --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--skip-shell"
    printf "    Skip injecting init into shell rc files for all tools\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "%s\n" "Arguments:"

    # :argument.usage
    printf "  %s\n" "TOOLS..."
    printf "    List of tools to configure (e.g., zoxide mcfly fzf)\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  gt setup multi zoxide mcfly fzf\n"
    printf "  gt setup multi --skip-shell zoxide mcfly\n"
    echo

  fi
}

# :command.usage
gt_setup_antidote_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt setup antidote - Configure antidote Zsh plugin manager (~/.zshrc and ~/.zsh_plugins.txt)\n"
    echo

  else
    printf "gt setup antidote - Configure antidote Zsh plugin manager (~/.zshrc and ~/.zsh_plugins.txt)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt setup antidote [OPTIONS]\n"
  printf "  gt setup antidote --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--skip-plugins"
    printf "    Skip seeding ~/.zsh_plugins.txt with recommended plugins\n"
    echo

    # :flag.usage
    printf "  %s\n" "--skip-zshrc"
    printf "    Skip adding antidote bootstrap to ~/.zshrc\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  gt setup antidote\n"
    printf "  gt setup antidote --skip-zshrc\n"
    echo

  fi
}

# :command.usage
gt_setup_starship_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt setup starship - Configure starship prompt (~/.config/starship.toml)\n"
    echo

  else
    printf "gt setup starship - Configure starship prompt (~/.config/starship.toml)\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt setup starship [OPTIONS]\n"
  printf "  gt setup starship --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--preset NAME"
    printf "    Apply a built-in starship preset (e.g. tokyo-night, pure-preset,\n    gruvbox-rainbow). Run \`starship preset list\` to see all.\n"
    echo

    # :flag.usage
    printf "  %s\n" "--skip-config"
    printf "    Skip seeding ~/.config/starship.toml\n"
    echo

    # :flag.usage
    printf "  %s\n" "--force-config"
    printf "    Overwrite ~/.config/starship.toml even if it already has content\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  gt setup starship --preset tokyo-night\n"
    printf "  gt setup starship --preset gruvbox-rainbow --force-config\n"
    echo

  fi
}

# :command.usage
gt_outdated_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt outdated - Check for outdated tools\n"
    echo

  else
    printf "gt outdated - Check for outdated tools\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt outdated [TOOL] [OPTIONS]\n"
  printf "  gt outdated --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--all, -a"
    printf "    Check all tools, even those not currently installed\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "%s\n" "Arguments:"

    # :argument.usage
    printf "  %s\n" "TOOL"
    printf "    Specific tool to check (optional)\n"
    echo

  fi
}

# :command.usage
gt_completions_usage() {
  if [[ -n $long_usage ]]; then
    printf "gt completions - Generate shell completion script\n"
    echo

  else
    printf "gt completions - Generate shell completion script\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  gt completions [SHELL]\n"
  printf "  gt completions --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "%s\n" "Arguments:"

    # :argument.usage
    printf "  %s\n" "SHELL"
    printf "    Shell type (bash or zsh)\n"
    printf "    Allowed: bash, zsh\n"
    printf "    Default: bash\n"
    echo

  fi
}

# :command.normalize_input
normalize_input() {
  local arg flags

  while [[ $# -gt 0 ]]; do
    arg="$1"
    if [[ $arg =~ ^(--[a-zA-Z0-9_\-]+)=(.+)$ ]]; then
      input+=("${BASH_REMATCH[1]}")
      input+=("${BASH_REMATCH[2]}")
    elif [[ $arg =~ ^(-[a-zA-Z0-9])=(.+)$ ]]; then
      input+=("${BASH_REMATCH[1]}")
      input+=("${BASH_REMATCH[2]}")
    elif [[ $arg =~ ^-([a-zA-Z0-9][a-zA-Z0-9]+)$ ]]; then
      flags="${BASH_REMATCH[1]}"
      for ((i = 0; i < ${#flags}; i++)); do
        input+=("-${flags:i:1}")
      done
    else
      input+=("$arg")
    fi

    shift
  done
}

# :command.inspect_args
inspect_args() {
  if ((${#args[@]})); then
    readarray -t sorted_keys < <(printf '%s\n' "${!args[@]}" | sort)
    echo args:
    for k in "${sorted_keys[@]}"; do
      echo "- \${args[$k]} = ${args[$k]}"
    done
  else
    echo args: none
  fi

  if ((${#other_args[@]})); then
    echo
    echo other_args:
    echo "- \${other_args[*]} = ${other_args[*]}"
    for i in "${!other_args[@]}"; do
      echo "- \${other_args[$i]} = ${other_args[$i]}"
    done
  fi

  if ((${#deps[@]})); then
    readarray -t sorted_keys < <(printf '%s\n' "${!deps[@]}" | sort)
    echo
    echo deps:
    for k in "${sorted_keys[@]}"; do
      echo "- \${deps[$k]} = ${deps[$k]}"
    done
  fi

  if ((${#env_var_names[@]})); then
    readarray -t sorted_names < <(printf '%s\n' "${env_var_names[@]}" | sort)
    echo
    echo "environment variables:"
    for k in "${sorted_names[@]}"; do
      echo "- \$$k = ${!k:-}"
    done
  fi
}

# :command.user_lib
# src/lib/colors.sh
print_in_color() {
  local color="$1"
  shift
  if [[ -z ${NO_COLOR+x} ]]; then
    printf "$color%b\e[0m\n" "$*"
  else
    printf "%b\n" "$*"
  fi
}

red() { print_in_color "\e[31m" "$*"; }
green() { print_in_color "\e[32m" "$*"; }
yellow() { print_in_color "\e[33m" "$*"; }
blue() { print_in_color "\e[34m" "$*"; }
magenta() { print_in_color "\e[35m" "$*"; }
cyan() { print_in_color "\e[36m" "$*"; }
bold() { print_in_color "\e[1m" "$*"; }
underlined() { print_in_color "\e[4m" "$*"; }
red_bold() { print_in_color "\e[1;31m" "$*"; }
green_bold() { print_in_color "\e[1;32m" "$*"; }
yellow_bold() { print_in_color "\e[1;33m" "$*"; }
blue_bold() { print_in_color "\e[1;34m" "$*"; }
magenta_bold() { print_in_color "\e[1;35m" "$*"; }
cyan_bold() { print_in_color "\e[1;36m" "$*"; }
red_underlined() { print_in_color "\e[4;31m" "$*"; }
green_underlined() { print_in_color "\e[4;32m" "$*"; }
yellow_underlined() { print_in_color "\e[4;33m" "$*"; }
blue_underlined() { print_in_color "\e[4;34m" "$*"; }
magenta_underlined() { print_in_color "\e[4;35m" "$*"; }
cyan_underlined() { print_in_color "\e[4;36m" "$*"; }

# src/lib/generic_install.sh
# Generic installer engine
# Centralizes download and extraction logic for all tools

_install_from_extracted_dir() {
  local name="$1" temp_dir="$2" bin_path="$3" target="$4"
  if [[ -f "$temp_dir/$bin_path" ]]; then
    install_binary "$temp_dir/$bin_path" "$target"
  else
    local found
    found=$(find "$temp_dir" -type f -name "$name" | head -n 1)
    if [[ -n "$found" ]]; then
      install_binary "$found" "$target"
    else
      echo "$(red "Could not find binary $name in archive")"
      ls -la "$temp_dir"
      rm -rf "$temp_dir"
      exit 1
    fi
  fi
}

run_generic_install() {
  local name="$1"
  local version="$2"
  local force="$3"
  local target="/usr/local/bin/$name"

  # Load tool configurations from its registry namespace
  local tool_upper
  tool_upper=$(echo "$name" | tr '[:lower:]' '[:upper:]' | tr '-' '_')

  local install_type_var="${tool_upper}_INSTALL_TYPE"
  local install_type="${!install_type_var:-github_release}"

  local repo_var="${tool_upper}_GITHUB_REPO"
  local repo="${!repo_var}"

  local archive_var="${tool_upper}_ARCHIVE_TYPE"
  local archive_type="${!archive_var:-binary}"

  local pattern_var="${tool_upper}_ASSET_PATTERN"
  local asset_pattern="${!pattern_var}"

  # Step 1: Guard against overwrite
  guard_existing "$name" "$target" "$force"

  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  if [[ "$install_type" == "github_release" ]]; then
    if [[ -z "$repo" ]]; then
      echo "$(red "Error: GitHub repo not defined for $name")"
      exit 1
    fi

    # Step 2: Resolve version
    if [[ "$version" == "latest" ]]; then
      if type "${name}_fetch_remote_version" &>/dev/null; then
        version=$("${name}_fetch_remote_version")
      else
        version=$(github_latest_tag "$repo")
      fi
    fi

    local dl_version="$version"
    if [[ "$dl_version" == v* ]]; then
      dl_version="${dl_version:1}"
    fi

    # Step 3: Detect platform
    detect_platform

    # Custom architecture mappings for some tools (e.g. some use x86_64 instead of amd64)
    local arch_mapped="$DETECT_ARCH"
    local arch_map_var="${tool_upper}_ARCH_MAP_${DETECT_ARCH}"
    local arch_map_val="${!arch_map_var}"
    if [[ -n "$arch_map_val" ]]; then
      arch_mapped="$arch_map_val"
    fi

    local os_mapped="$DETECT_OS"
    local os_map_var="${tool_upper}_OS_MAP_${DETECT_OS}"
    local os_map_val="${!os_map_var}"
    if [[ -n "$os_map_val" ]]; then
      os_mapped="$os_map_val"
    fi

    local asset_name="$asset_pattern"
    asset_name="${asset_name//\$\{DETECT_OS\}/$os_mapped}"
    asset_name="${asset_name//\$\{DETECT_ARCH\}/$arch_mapped}"
    asset_name="${asset_name//\$\{VERSION\}/$dl_version}"
    asset_name="${asset_name//\$\{V_VERSION\}/v$dl_version}"

    # Determine tag for download URL
    local tag_prefix_var="${tool_upper}_TAG_PREFIX"
    local tag_prefix="${!tag_prefix_var-v}"

    # If the tag from API already includes the prefix, don't duplicate it.
    # But since github_latest_tag returns exact tag, if user provided 'latest', it is exact.
    # If user provided version manually (e.g. 1.2.3), we might need to add prefix.
    local download_tag="$version"
    if [[ "$version" != "latest" && "$version" != v* && -n "$tag_prefix" ]]; then
       download_tag="${tag_prefix}${version}"
    fi

    local download_url="https://github.com/${repo}/releases/download/${download_tag}/${asset_name}"

    local url_var="${tool_upper}_DOWNLOAD_URL"
    local custom_url="${!url_var}"
    if [[ -n "$custom_url" ]]; then
      download_url="$custom_url"
      download_url="${download_url//\$\{DETECT_OS\}/$os_mapped}"
      download_url="${download_url//\$\{DETECT_ARCH\}/$arch_mapped}"
      download_url="${download_url//\$\{VERSION\}/$dl_version}"
      download_url="${download_url//\$\{V_VERSION\}/v$dl_version}"
      download_url="${download_url//\$\{TAG\}/$download_tag}"
    fi

    # Step 4: Download
    local temp_dir
    temp_dir=$(mktemp -d)

    if [[ "$archive_type" == "binary" ]]; then
      download_file "$download_url" "$temp_dir/$name"
      install_binary "$temp_dir/$name" "$target"
    elif [[ "$archive_type" == "tar.gz" ]]; then
      download_file "$download_url" "$temp_dir/archive.tar.gz"
      tar -xzf "$temp_dir/archive.tar.gz" -C "$temp_dir"
      local bin_path_var="${tool_upper}_BIN_PATH"
      local bin_path="${!bin_path_var:-$name}"
      bin_path="${bin_path//\$\{DETECT_OS\}/$os_mapped}"
      bin_path="${bin_path//\$\{DETECT_ARCH\}/$arch_mapped}"
      bin_path="${bin_path//\$\{VERSION\}/$dl_version}"
      bin_path="${bin_path//\$\{V_VERSION\}/v$dl_version}"
      _install_from_extracted_dir "$name" "$temp_dir" "$bin_path" "$target"
    elif [[ "$archive_type" == "zip" ]]; then
      download_file "$download_url" "$temp_dir/archive.zip"
      unzip -q "$temp_dir/archive.zip" -d "$temp_dir"
      local bin_path_var="${tool_upper}_BIN_PATH"
      local bin_path="${!bin_path_var:-$name}"
      bin_path="${bin_path//\$\{DETECT_OS\}/$os_mapped}"
      bin_path="${bin_path//\$\{DETECT_ARCH\}/$arch_mapped}"
      bin_path="${bin_path//\$\{VERSION\}/$dl_version}"
      bin_path="${bin_path//\$\{V_VERSION\}/v$dl_version}"
      _install_from_extracted_dir "$name" "$temp_dir" "$bin_path" "$target"
    fi
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "v${dl_version}")"

  elif [[ "$install_type" == "pip" ]]; then
    local pip_pkg_var="${tool_upper}_PIP_PKG"
    local pip_pkg="${!pip_pkg_var:-$name}"

    if [[ "$version" != "latest" ]]; then
      local dl_version="$version"
      if [[ "$dl_version" == v* ]]; then
        dl_version="${dl_version:1}"
      fi
      pip_pkg="$pip_pkg==$dl_version"
    fi

    if ! command -v pip3 >/dev/null 2>&1; then
      echo "$(red "Error: pip3 is not installed. Please install python3-pip first.")"
      exit 1
    fi

    echo "Running pip3 install..."
    if sudo pip3 install --upgrade "$pip_pkg"; then
      echo "$(green_bold ✓) $name installed successfully!"
    else
      echo "$(red ✗ Failed to install $name.)"
      exit 1
    fi
  fi
}

# src/lib/generic_uninstall.sh
run_generic_uninstall() {
  local name="$1"
  local target="/usr/local/bin/$name"

  local tool_upper
  tool_upper=$(echo "$name" | tr '[:lower:]' '[:upper:]' | tr '-' '_')

  local install_type_var="${tool_upper}_INSTALL_TYPE"
  local install_type="${!install_type_var:-github_release}"

  if [[ "$install_type" == "pip" ]]; then
    local pip_target
    pip_target=$(command -v "$name")

    if [ -z "$pip_target" ]; then
      echo "$(cyan_bold "$name") is not installed."
      exit 0
    fi

    echo "Uninstalling $(cyan_bold "$name")..."
    if sudo pip3 uninstall -y "$name"; then
      echo "$(green_bold ✓) $name uninstalled successfully."
    else
      echo "$(red ✗ Failed to uninstall $name.)"
      exit 1
    fi
  else
    # Standard binary uninstall
    uninstall_tool "$name" "$target"
  fi
}

# src/lib/install_helpers.sh

# Detect OS and ARCH, sets global vars: DETECT_OS, DETECT_ARCH
detect_platform() {
  local raw_os raw_arch
  raw_os="$(uname -s)"
  raw_arch="$(uname -m)"

  case "$raw_os" in
    Linux)  DETECT_OS="linux" ;;
    Darwin) DETECT_OS="darwin" ;;
    *)      echo "$(red Error:) Unsupported OS: $raw_os"; exit 1 ;;
  esac

  case "$raw_arch" in
    x86_64)  DETECT_ARCH="amd64" ;;
    aarch64) DETECT_ARCH="arm64" ;;
    arm64)   DETECT_ARCH="arm64" ;;
    *)       echo "$(red Error:) Unsupported architecture: $raw_arch"; exit 1 ;;
  esac
}

# Fetch the latest release tag from a GitHub repo
# Usage: github_latest_tag "owner/repo"
# Returns: tag string (e.g. "v0.50.18")
github_latest_tag() {
  local repo="$1"
  local tag=""
  local api_url="https://api.github.com/repos/${repo}/releases/latest"
  local api_opts=(-s "$api_url")

  if [[ -n "$GITHUB_TOKEN" ]]; then
    api_opts=(-s -H "Authorization: Bearer $GITHUB_TOKEN" "$api_url")
  fi

  tag=$(curl "${api_opts[@]}" | grep '"tag_name":' | sed -E 's/.*"([^"]+)".*/\1/')

  # If API fails (e.g. rate limit), fallback to following the /releases/latest redirect
  if [[ -z "$tag" ]]; then
    local url
    url=$(curl -sLI -o /dev/null -w '%{url_effective}' "https://github.com/${repo}/releases/latest")
    tag="${url##*/}"
    if [[ "$tag" == "latest" || "$tag" == "releases" ]]; then
      tag=""
    fi
  fi

  if [ -z "$tag" ]; then
    echo "$(red Error:) Could not fetch latest version for ${repo}." >&2
    exit 1
  fi

  echo "$tag"
}

# Guard: check if a binary already exists, exit unless --force is set
# Usage: guard_existing "tool_name" "/path/to/binary" "$force_flag"
guard_existing() {
  local name="$1" target="$2" force="$3"
  if [ -f "$target" ] && [ -z "$force" ]; then
    echo "$(red Error:) ${name} is already installed at ${target}."
    echo "Use $(bold --force) (or $(bold -f)) to overwrite."
    exit 1
  fi
}

# Require: check if a tool is installed, exit if it is not
# Usage: require_installed "tool_name" "/path/to/binary_or_dir"
require_installed() {
  local name="$1" target="$2"
  if [ ! -e "$target" ] && ! command -v "$name" >/dev/null 2>&1; then
    echo "$(red "✗") ${name} is not installed."
    echo "Please run $(bold "./gt install ${name}") first."
    exit 1
  fi
}

# Move a file to /usr/local/bin, using sudo if necessary
# Usage: install_binary "/tmp/downloaded_binary" "/usr/local/bin/tool"
install_binary() {
  local src="$1" dest="$2"
  chmod +x "$src"
  if [ -w "$(dirname "$dest")" ]; then
    mv -f "$src" "$dest"
  else
    sudo mv -f "$src" "$dest"
  fi
}

# Remove a binary from a path, using sudo if necessary
# Usage: remove_binary "/usr/local/bin/tool"
remove_binary() {
  local target="$1"
  if [ -w "$(dirname "$target")" ]; then
    rm -f "$target"
  else
    sudo rm -f "$target"
  fi
}

# Download a file with a progress bar
# Usage: download_file "https://example.com/file.tar.gz" "/tmp/output.tar.gz"
download_file() {
  local url="$1" dest="$2"
  curl -#fL "$url" -o "$dest"
  if [ $? -ne 0 ] || [ ! -f "$dest" ]; then
    echo "$(red Error:) Failed to download from ${url}"
    rm -rf "$(dirname "$dest")"
    exit 1
  fi
}

# Standard uninstall flow for a single binary
# Usage: uninstall_tool "tool_name" "/usr/local/bin/tool"
uninstall_tool() {
  local name="$1" target="$2"

  if [ ! -f "$target" ]; then
    echo "$(yellow ${name} is not installed) at ${target}."
    exit 0
  fi

  echo -n "Are you sure you want to uninstall $(cyan_bold "${name}") at $target? [y/N] "
  read -r response
  if [[ ! "$response" =~ ^[Yy]$ ]]; then
    echo "Aborted."
    exit 0
  fi

  echo "Uninstalling $(cyan_bold "${name}")..."
  remove_binary "$target"

  if [ ! -f "$target" ]; then
    echo "$(green_bold ✓) ${name} uninstalled successfully."
  else
    echo "$(red ✗ Failed to uninstall ${name}.)"
    exit 1
  fi
}

# Install a package using the system package manager
# Usage: install_package "pkg_name"
install_package() {
  local pkg="$1"
  if command -v apt-get >/dev/null 2>&1; then
    sudo apt-get update -yqq
    sudo apt-get install -y "$pkg"
  elif command -v dnf >/dev/null 2>&1; then
    sudo dnf install -y "$pkg"
  elif command -v yum >/dev/null 2>&1; then
    sudo yum install -y "$pkg"
  elif command -v pacman >/dev/null 2>&1; then
    sudo pacman -S --noconfirm "$pkg"
  elif command -v apk >/dev/null 2>&1; then
    sudo apk add "$pkg"
  else
    echo "$(red ✗) Could not detect a supported package manager (apt/dnf/yum/pacman/apk)."
    return 1
  fi
}

# Remove a package using the system package manager
# Usage: remove_package "pkg_name"
remove_package() {
  local pkg="$1"
  if command -v apt-get >/dev/null 2>&1; then
    sudo apt-get remove -y "$pkg"
  elif command -v dnf >/dev/null 2>&1; then
    sudo dnf remove -y "$pkg"
  elif command -v yum >/dev/null 2>&1; then
    sudo yum remove -y "$pkg"
  elif command -v pacman >/dev/null 2>&1; then
    sudo pacman -Rs --noconfirm "$pkg"
  elif command -v apk >/dev/null 2>&1; then
    sudo apk del "$pkg"
  else
    echo "$(red ✗) Could not detect a supported package manager."
    return 1
  fi
}

# Install a tool by cloning a git repository
# Usage: install_git_repo "https://github.com/user/repo.git" "/dest/path" "branch_or_tag"
install_git_repo() {
  local repo_url="$1" dest="$2" version="$3"

  if [ -d "$dest" ]; then
    echo "Directory $dest already exists. Pulling latest changes..."
    if [ ! -w "$dest" ]; then
      sudo git -C "$dest" fetch --tags
      if [ -n "$version" ] && [ "$version" != "latest" ]; then
        sudo git -C "$dest" checkout "$version"
      else
        sudo git -C "$dest" pull
      fi
    else
      git -C "$dest" fetch --tags
      if [ -n "$version" ] && [ "$version" != "latest" ]; then
        git -C "$dest" checkout "$version"
      else
        git -C "$dest" pull
      fi
    fi
  else
    echo "Cloning $repo_url to $dest..."
    local parent_dir="$(dirname "$dest")"
    if [ ! -w "$parent_dir" ]; then
      sudo mkdir -p "$parent_dir"
      if [ -n "$version" ] && [ "$version" != "latest" ]; then
        sudo git clone --depth=1 --branch "$version" "$repo_url" "$dest"
      else
        sudo git clone --depth=1 "$repo_url" "$dest"
      fi
    else
      mkdir -p "$parent_dir"
      if [ -n "$version" ] && [ "$version" != "latest" ]; then
        git clone --depth=1 --branch "$version" "$repo_url" "$dest"
      else
        git clone --depth=1 "$repo_url" "$dest"
      fi
    fi
  fi
}

# Remove a git repository directory
# Usage: remove_git_repo "/dest/path"
remove_git_repo() {
  local dest="$1"
  if [ -d "$dest" ]; then
    if [ ! -w "$(dirname "$dest")" ]; then
      sudo rm -rf "$dest"
    else
      rm -rf "$dest"
    fi
  else
    echo "$(yellow Directory $dest does not exist.)"
  fi
}

# src/lib/registry/age.sh
AGE_INSTALL_TYPE="github_release"
AGE_GITHUB_REPO="FiloSottile/age"
AGE_ARCHIVE_TYPE="tar.gz"
AGE_ASSET_PATTERN="age-v\${VERSION}-\${DETECT_OS}-\${DETECT_ARCH}.tar.gz"
AGE_BIN_PATH="age/age"

# src/lib/registry/antidote.sh
ANTIDOTE_GITHUB_REPO="mattmc3/antidote"

antidote_fetch_local_version() {
  local target="/usr/local/share/antidote/.bumpversion.cfg"
  if [ -f "$target" ]; then
    grep "current_version =" "$target" | awk -F'=' '{print $2}' | tr -d ' \r\n'
  else
    echo ""
  fi
}

# src/lib/registry/argocd.sh
ARGOCD_INSTALL_TYPE="github_release"
ARGOCD_GITHUB_REPO="argoproj/argo-cd"
ARGOCD_ARCHIVE_TYPE="binary"
ARGOCD_ASSET_PATTERN="argocd-\${DETECT_OS}-\${DETECT_ARCH}"

# src/lib/registry/bashly.sh
bashly_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null
}

bashly_fetch_remote_version() {
  curl -s "https://rubygems.org/api/v1/versions/bashly/latest.json" | grep -o '"version":"[^"]*"' | cut -d'"' -f4
}

# src/lib/registry/bat.sh
BAT_GITHUB_REPO="sharkdp/bat"

bat_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $2}'
}

# src/lib/registry/btop.sh
BTOP_GITHUB_REPO="aristocratos/btop"
BTOP_ARCHIVE_TYPE="tar.gz"
BTOP_ASSET_PATTERN="btop-\${DETECT_ARCH}-unknown-\${DETECT_OS}-musl.tar.gz"
BTOP_ARCH_MAP_amd64="x86_64"
BTOP_ARCH_MAP_arm64="aarch64"
BTOP_OS_MAP_linux="linux"
BTOP_BIN_PATH="btop/bin/btop"

btop_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | head -n 1 | awk '{print $3}' | cut -d'+' -f1 | sed 's/\x1b\[[0-9;]*m//g' | tr -d ' \r\n'
}

# src/lib/registry/chezmoi.sh
CHEZMOI_GITHUB_REPO="twpayne/chezmoi"

chezmoi_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $3}'
}

# src/lib/registry/claude.sh
CLAUDE_INSTALL_TYPE="npm"
CLAUDE_NPM_PKG="@anthropic-ai/claude-code"

claude_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $1}'
}

claude_fetch_remote_version() {
  npm view @anthropic-ai/claude-code version 2>/dev/null
}

# src/lib/registry/conftest.sh
CONFTEST_INSTALL_TYPE="github_release"
CONFTEST_GITHUB_REPO="open-policy-agent/conftest"
CONFTEST_ARCHIVE_TYPE="tar.gz"
CONFTEST_ASSET_PATTERN="conftest_\${VERSION}_\${DETECT_OS}_x86_64.tar.gz"
CONFTEST_ARCH_MAP_amd64="x86_64"

# src/lib/registry/direnv.sh
DIRENV_GITHUB_REPO="direnv/direnv"
DIRENV_ARCHIVE_TYPE="binary"
DIRENV_ASSET_PATTERN="direnv.\${DETECT_OS}-\${DETECT_ARCH}"

direnv_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null
}

# src/lib/registry/eza.sh
EZA_GITHUB_REPO="eza-community/eza"

eza_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | head -1 | awk '{print $2}'
}

eza_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk 'NR==2 {print $1}'
}

# src/lib/registry/fastfetch.sh
FASTFETCH_GITHUB_REPO="fastfetch-cli/fastfetch"
FASTFETCH_ARCHIVE_TYPE="tar.gz"
FASTFETCH_ASSET_PATTERN="fastfetch-\${DETECT_OS}-\${DETECT_ARCH}.tar.gz"
FASTFETCH_ARCH_MAP_arm64="aarch64"
FASTFETCH_OS_MAP_darwin="macos"
FASTFETCH_TAG_PREFIX=""

fastfetch_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $2}'
}

# src/lib/registry/fd.sh
FD_GITHUB_REPO="sharkdp/fd"

fd_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $2}'
}

# src/lib/registry/fzf.sh
FZF_GITHUB_REPO="junegunn/fzf"

fzf_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $1}'
}

# src/lib/registry/gem.sh
gem_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null
}

gem_fetch_remote_version() {
  # Package manager tools: return local version so they appear up-to-date
  if command -v gem >/dev/null 2>&1; then
    gem --version 2>/dev/null
  fi
}

# src/lib/registry/gum.sh
GUM_GITHUB_REPO="charmbracelet/gum"

gum_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $3}'
}

# src/lib/registry/helm.sh
HELM_GITHUB_REPO="helm/helm"

helm_fetch_local_version() {
  local target="$1"
  "$target" version --short 2>/dev/null | awk -F+ '{print $1}'
}

# src/lib/registry/helmfile.sh
HELMFILE_GITHUB_REPO="helmfile/helmfile"

helmfile_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $3}'
}

# src/lib/registry/hyperfine.sh
HYPERFINE_GITHUB_REPO="sharkdp/hyperfine"

hyperfine_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $2}'
}

# src/lib/registry/infisical.sh
INFISICAL_GITHUB_REPO="Infisical/cli"

infisical_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $3}'
}

# src/lib/registry/jq.sh
JQ_GITHUB_REPO="jqlang/jq"

jq_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $1}'
}

# src/lib/registry/k9s.sh
K9S_GITHUB_REPO="derailed/k9s"
K9S_ARCHIVE_TYPE="tar.gz"
K9S_ASSET_PATTERN="k9s_\${DETECT_OS}_\${DETECT_ARCH}.tar.gz"
K9S_OS_MAP_linux="Linux"
K9S_OS_MAP_darwin="Darwin"

k9s_fetch_local_version() {
  local target="$1"
  "$target" version -s 2>/dev/null | awk '/Version/ {print $2}'
}

# src/lib/registry/krew.sh
KREW_INSTALL_TYPE="github_release"
KREW_GITHUB_REPO="kubernetes-sigs/krew"
KREW_ARCHIVE_TYPE="tar.gz"
KREW_ASSET_PATTERN="krew-\${DETECT_OS}_\${DETECT_ARCH}.tar.gz"
KREW_BIN_PATH="krew-\${DETECT_OS}_\${DETECT_ARCH}"

# src/lib/registry/ksops.sh
KSOPS_GITHUB_REPO="viaduct-ai/kustomize-sops"

ksops_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $3}'
}

# src/lib/registry/kube-linter.sh
KUBE_LINTER_INSTALL_TYPE="github_release"
KUBE_LINTER_GITHUB_REPO="stackrox/kube-linter"
KUBE_LINTER_ARCHIVE_TYPE="tar.gz"
KUBE_LINTER_ASSET_PATTERN="kube-linter-\${DETECT_OS}.tar.gz"

# src/lib/registry/kubecolor.sh
KUBECOLOR_INSTALL_TYPE="github_release"
KUBECOLOR_GITHUB_REPO="kubecolor/kubecolor"
KUBECOLOR_ARCHIVE_TYPE="tar.gz"
KUBECOLOR_ASSET_PATTERN="kubecolor_\${VERSION}_\${DETECT_OS}_\${DETECT_ARCH}.tar.gz"

# src/lib/registry/kubeconform.sh
KUBECONFORM_INSTALL_TYPE="github_release"
KUBECONFORM_GITHUB_REPO="yannh/kubeconform"
KUBECONFORM_ARCHIVE_TYPE="tar.gz"
KUBECONFORM_ASSET_PATTERN="kubeconform-\${DETECT_OS}-\${DETECT_ARCH}.tar.gz"

# src/lib/registry/kubectl.sh
KUBECTL_GITHUB_REPO="kubernetes/kubernetes"
KUBECTL_ARCHIVE_TYPE="binary"
KUBECTL_DOWNLOAD_URL="https://dl.k8s.io/release/\${V_VERSION}/bin/\${DETECT_OS}/\${DETECT_ARCH}/kubectl"

kubectl_fetch_local_version() {
  local target="$1"
  "$target" version --client -o yaml 2>/dev/null | grep gitVersion | awk '{print $2}'
}

kubectl_fetch_remote_version() {
  curl -sL https://dl.k8s.io/release/stable.txt
}

# src/lib/registry/kubens.sh
KUBENS_INSTALL_TYPE="github_release"
KUBENS_GITHUB_REPO="ahmetb/kubectx"
KUBENS_ARCHIVE_TYPE="tar.gz"
KUBENS_ASSET_PATTERN="kubens_v\${VERSION}_\${DETECT_OS}_x86_64.tar.gz"
KUBENS_ARCH_MAP_amd64="x86_64"

# src/lib/registry/kubent.sh
KUBENT_INSTALL_TYPE="github_release"
KUBENT_GITHUB_REPO="doitintl/kube-no-trouble"
KUBENT_ARCHIVE_TYPE="tar.gz"
KUBENT_ASSET_PATTERN="kubent-\${VERSION}-\${DETECT_OS}-\${DETECT_ARCH}.tar.gz"

# src/lib/registry/kubeseal.sh
KUBESEAL_INSTALL_TYPE="github_release"
KUBESEAL_GITHUB_REPO="bitnami-labs/sealed-secrets"
KUBESEAL_ARCHIVE_TYPE="tar.gz"
KUBESEAL_ASSET_PATTERN="kubeseal-\${VERSION}-\${DETECT_OS}-\${DETECT_ARCH}.tar.gz"

# src/lib/registry/kustomize.sh
KUSTOMIZE_GITHUB_REPO="kubernetes-sigs/kustomize"
KUSTOMIZE_ARCHIVE_TYPE="tar.gz"
KUSTOMIZE_ASSET_PATTERN="kustomize_\${V_VERSION}_\${DETECT_OS}_\${DETECT_ARCH}.tar.gz"
KUSTOMIZE_TAG_PREFIX="kustomize/v"

kustomize_fetch_local_version() {
  local target="$1"
  local ver=$("$target" version --short 2>/dev/null || "$target" version)
  echo "$ver" | grep -oE 'v[0-9]+\.[0-9]+\.[0-9]+' | head -n 1
}

kustomize_fetch_remote_version() {
  local api_opts=(-s "https://api.github.com/repos/${KUSTOMIZE_GITHUB_REPO}/releases/latest")
  if [[ -n "$GITHUB_TOKEN" ]]; then
    api_opts=(-s -H "Authorization: Bearer $GITHUB_TOKEN" "https://api.github.com/repos/${KUSTOMIZE_GITHUB_REPO}/releases/latest")
  fi
  curl "${api_opts[@]}" | grep '"tag_name":' | sed -E 's/.*"kustomize\/v?([^"]+)".*/\1/'
}

# src/lib/registry/kyverno.sh
KYVERNO_INSTALL_TYPE="github_release"
KYVERNO_GITHUB_REPO="kyverno/kyverno"
KYVERNO_ARCHIVE_TYPE="tar.gz"
KYVERNO_ASSET_PATTERN="kyverno-cli_v\${VERSION}_\${DETECT_OS}_x86_64.tar.gz"
KYVERNO_BIN_PATH="kyverno"
KYVERNO_ARCH_MAP_amd64="x86_64"

# src/lib/registry/mcfly.sh
MCFLY_GITHUB_REPO="cantino/mcfly"

mcfly_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $2}'
}

# src/lib/registry/nerdctl.sh
NERDCTL_GITHUB_REPO="containerd/nerdctl"

nerdctl_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $3}'
}

# src/lib/registry/popeye.sh
POPEYE_INSTALL_TYPE="github_release"
POPEYE_GITHUB_REPO="derailed/popeye"
POPEYE_ARCHIVE_TYPE="tar.gz"
POPEYE_ASSET_PATTERN="popeye_\${DETECT_OS}_\${DETECT_ARCH}.tar.gz"

# src/lib/registry/ripgrep.sh
RIPGREP_GITHUB_REPO="BurntSushi/ripgrep"
RIPGREP_INSTALL_TARGET="/usr/local/bin/rg"

ripgrep_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | head -1 | awk '{print $2}'
}

# src/lib/registry/rsync.sh
rsync_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | head -n 1 | awk '{print $3}'
}

rsync_fetch_remote_version() {
  if command -v rsync >/dev/null 2>&1; then
    rsync --version 2>/dev/null | head -n 1 | awk '{print $3}'
  fi
}

# src/lib/registry/skopeo.sh
skopeo_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $3}'
}

skopeo_fetch_remote_version() {
  if command -v skopeo >/dev/null 2>&1; then
    skopeo --version 2>/dev/null | awk '{print $3}'
  fi
}

# src/lib/registry/sops.sh
SOPS_INSTALL_TYPE="github_release"
SOPS_GITHUB_REPO="getsops/sops"
SOPS_ARCHIVE_TYPE="binary"
SOPS_ASSET_PATTERN="sops-v\${VERSION}.\${DETECT_OS}.\${DETECT_ARCH}"

# src/lib/registry/starship.sh
STARSHIP_GITHUB_REPO="starship/starship"

starship_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $2}'
}

starship_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | head -n1 | awk '{print $2}'
}

# src/lib/registry/talisman.sh
TALISMAN_GITHUB_REPO="thoughtworks/talisman"

talisman_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $2}'
}

# src/lib/registry/thefuck.sh
THEFUCK_INSTALL_TYPE="pip"
THEFUCK_PIP_PKG="thefuck"

thefuck_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $2}'
}

thefuck_fetch_remote_version() {
  curl -s "https://pypi.org/pypi/thefuck/json" | sed -n 's/.*"version":"\([^"]*\)".*/\1/p' | head -n 1
}

# src/lib/registry/tmux.sh
tmux_fetch_local_version() {
  local target="$1"
  "$target" -V 2>/dev/null | awk '{print $2}'
}

tmux_fetch_remote_version() {
  if command -v tmux >/dev/null 2>&1; then
    tmux -V 2>/dev/null | awk '{print $2}'
  fi
}

# src/lib/registry/trino.sh
TRINO_GITHUB_REPO="trinodb/trino"

trino_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $3}'
}

# src/lib/registry/trivy.sh
TRIVY_INSTALL_TYPE="github_release"
TRIVY_GITHUB_REPO="aquasecurity/trivy"
TRIVY_ARCHIVE_TYPE="tar.gz"
TRIVY_ASSET_PATTERN="trivy_\${VERSION}_\${DETECT_OS}-\${DETECT_ARCH}.tar.gz"
TRIVY_OS_MAP_linux="Linux"
TRIVY_OS_MAP_darwin="macOS"
TRIVY_ARCH_MAP_amd64="64bit"
TRIVY_ARCH_MAP_arm64="ARM64"

trivy_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | head -n1 | awk '{print $2}'
}

# src/lib/registry/uv.sh
UV_GITHUB_REPO="astral-sh/uv"
UV_ARCHIVE_TYPE="tar.gz"
UV_ASSET_PATTERN="uv-\${DETECT_ARCH}-unknown-\${DETECT_OS}-gnu.tar.gz"
UV_ARCH_MAP_amd64="x86_64"
UV_ARCH_MAP_arm64="aarch64"
UV_BIN_PATH="uv-\${DETECT_ARCH}-unknown-\${DETECT_OS}-gnu/uv"

uv_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $2}'
}

# src/lib/registry/vault.sh
VAULT_INSTALL_TYPE="github_release"
VAULT_GITHUB_REPO="hashicorp/vault"
VAULT_ARCHIVE_TYPE="zip"
VAULT_ASSET_PATTERN="vault_\${VERSION}_\${DETECT_OS}_\${DETECT_ARCH}.zip"
VAULT_DOWNLOAD_URL="https://releases.hashicorp.com/vault/\${VERSION}/vault_\${VERSION}_\${DETECT_OS}_\${DETECT_ARCH}.zip"

# src/lib/registry/velero.sh
VELERO_INSTALL_TYPE="github_release"
VELERO_GITHUB_REPO="vmware-tanzu/velero"
VELERO_ARCHIVE_TYPE="tar.gz"
VELERO_ASSET_PATTERN="velero-v\${VERSION}-\${DETECT_OS}-\${DETECT_ARCH}.tar.gz"
VELERO_BIN_PATH="velero-v\${VERSION}-\${DETECT_OS}-\${DETECT_ARCH}/velero"

# src/lib/registry/viddy.sh
VIDDY_GITHUB_REPO="sachaos/viddy"

viddy_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $3}'
}

# src/lib/registry/yq.sh
YQ_GITHUB_REPO="mikefarah/yq"

yq_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $NF}'
}

# src/lib/registry/zoxide.sh
ZOXIDE_GITHUB_REPO="ajeetdsouza/zoxide"

zoxide_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $2}'
}

# src/lib/registry/zsh.sh
zsh_fetch_local_version() {
  local target="$1"
  "$target" --version 2>/dev/null | awk '{print $2}' | tr -d ' \r\n'
}

zsh_fetch_remote_version() {
  # Since zsh is managed by OS package manager, we return the local version

  # so it always appears up-to-date, as remote versions depend on the distro package cache.
  if command -v zsh >/dev/null 2>&1; then
    zsh --version 2>/dev/null | awk '{print $2}' | tr -d ' \r\n'
  else
    echo ""
  fi
}

# src/lib/send_completions.sh
send_completions() {
  echo $'# gt completion                                            -*- shell-script -*-'
  echo $''
  echo $'# This bash completions script was generated by'
  echo $'# completely (https://github.com/dannyben/completely)'
  echo $'# Modifying it manually is not recommended'
  echo $''
  echo $'_gt_completions_filter() {'
  echo $'  local words="$1"'
  echo $'  local cur=${COMP_WORDS[COMP_CWORD]}'
  echo $'  local result=()'
  echo $''
  echo $'  if [[ "${cur:0:1}" == "-" ]]; then'
  echo $'    echo "$words"'
  echo $''
  echo $'  else'
  echo $'    for word in $words; do'
  echo $'      [[ "${word:0:1}" != "-" ]] && result+=("$word")'
  echo $'    done'
  echo $''
  echo $'    echo "${result[*]}"'
  echo $''
  echo $'  fi'
  echo $'}'
  echo $''
  echo $'_gt_completions() {'
  echo $'  local cur=${COMP_WORDS[COMP_CWORD]}'
  echo $'  local compwords=("${COMP_WORDS[@]:1:$COMP_CWORD-1}")'
  echo $'  local compline="${compwords[*]}"'
  echo $''
  echo $'  case "$compline" in'
  echo $'    \'uninstall kubeconform\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall kube-linter\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install kubeconform\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install kube-linter\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall kustomize\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall fastfetch\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall kubecolor\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall infisical\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall hyperfine\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall antidote\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall conftest\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall kubeseal\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall starship\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall helmfile\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall talisman\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install kustomize\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install fastfetch\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install kubecolor\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install infisical\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install hyperfine\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall kubectl\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall thefuck\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall kyverno\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall ripgrep\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall chezmoi\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall nerdctl\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install antidote\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install conftest\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install kubeseal\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install starship\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install helmfile\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install talisman\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall direnv\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall argocd\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall velero\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall kubent\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall kubens\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall popeye\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall zoxide\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall skopeo\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall bashly\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall claude\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install kubectl\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install thefuck\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install kyverno\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install ripgrep\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install chezmoi\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install nerdctl\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall trivy\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall vault\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall mcfly\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall ksops\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall rsync\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall trino\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall viddy\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install direnv\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install argocd\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install velero\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install kubent\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install kubens\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install popeye\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install zoxide\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install skopeo\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help -f -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install bashly\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install claude\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall btop\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall krew\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall sops\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall helm\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall tmux\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'setup antidote\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help --skip-plugins --skip-zshrc -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'setup starship\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force-config --help --preset --skip-config -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install multi\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help -f -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install trivy\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install vault\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install mcfly\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install ksops\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install rsync\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help -f -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install trino\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install viddy\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall k9s\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall zsh\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall age\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall eza\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall bat\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall fzf\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall gum\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall gem\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install btop\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install krew\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install sops\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install helm\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install tmux\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help -f -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall fd\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall jq\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall yq\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall uv\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install k9s\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install zsh\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install age\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install eza\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install bat\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install fzf\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install gum\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install gem\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help -f -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'setup multi\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help --skip-shell -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install fd\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install jq\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install yq\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install uv\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--force --help --version -f -h -v")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'uninstall\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h age antidote argocd bashly bat btop chezmoi claude conftest direnv eza fastfetch fd fzf gem gum helm helmfile hyperfine infisical jq k9s krew ksops kube-linter kubecolor kubeconform kubectl kubens kubent kubeseal kustomize kyverno mcfly nerdctl popeye ripgrep rsync skopeo sops starship talisman thefuck tmux trino trivy uv vault velero viddy yq zoxide zsh")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'outdated\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--all --help -a -h")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'install\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h age antidote argocd bashly bat btop chezmoi claude conftest direnv eza fastfetch fd fzf gem gum helm helmfile hyperfine infisical jq k9s krew ksops kube-linter kubecolor kubeconform kubectl kubens kubent kubeseal kustomize kyverno mcfly multi nerdctl popeye ripgrep rsync skopeo sops starship talisman thefuck tmux trino trivy uv vault velero viddy yq zoxide zsh")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    \'setup\'*)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help -h antidote multi starship")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'    *)'
  echo $'      while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "$(_gt_completions_filter "--help --version -h -v install outdated setup uninstall")" -- "$cur")'
  echo $'      ;;'
  echo $''
  echo $'  esac'
  echo $'} &&'
  echo $'  complete -F _gt_completions gt'
  echo $''
  echo $'# ex: filetype=sh'
}

# src/lib/setup_helpers.sh

# Inject a block of text into a file if a marker is not already present
# Usage: inject_config_block "$target_file" "$marker" "$content"
inject_config_block() {
  local target_file="$1" marker="$2" content="$3"

  if grep -qF "$marker" "$target_file" 2>/dev/null; then
    echo "$(yellow "⚠") Config block already exists in $target_file — skipping"
    return 0
  fi

  # Create file if it doesn't exist
  [ -f "$target_file" ] || touch "$target_file"

  # Append the content
  printf '\n%s\n' "$content" >> "$target_file"
  echo "$(green_bold "✓") Config block added to $target_file"
}

# Inject a tool's shell-init block into both ~/.zshrc and ~/.bashrc.
# Skips files that don't exist (i.e. shells the user doesn't use). Idempotent
# via the marker passed to inject_config_block.
# Usage: setup_shell_tool "name" "$marker" "$zsh_block" "$bash_block"
setup_shell_tool() {
  local name="$1" marker="$2" zsh_block="$3" bash_block="$4"
  local zshrc="${ZDOTDIR:-$HOME}/.zshrc"
  local bashrc="$HOME/.bashrc"

  if [ -f "$zshrc" ]; then
    inject_config_block "$zshrc" "$marker" "$zsh_block"
  else
    echo "$(yellow "⚠") $zshrc does not exist — skipping zsh"
  fi

  if [ -f "$bashrc" ]; then
    inject_config_block "$bashrc" "$marker" "$bash_block"
  else
    echo "$(yellow "⚠") $bashrc does not exist — skipping bash"
  fi
}

# Seed a file with content if the file is empty or does not exist
# Usage: seed_file "$target_file" "$content"
seed_file() {
  local target_file="$1" content="$2"

  if [ -s "$target_file" ]; then
    echo "$(yellow "⚠") $target_file already has content — skipping (delete it first to re-seed)"
    return 0
  fi

  # Create parent directories if they don't exist
  mkdir -p "$(dirname "$target_file")"

  # Write content
  printf '%s\n' "$content" > "$target_file"
  echo "$(green_bold "✓") Seeded $target_file"
}

# src/lib/templates/antidote.sh

# Returns the bootstrap block to be injected into ~/.zshrc
template_antidote_zshrc() {
  cat <<'EOF'
# --- antidote bootstrap (managed by os-scripts) ---
for _antidote_path in \
  /opt/homebrew/opt/antidote/share/antidote/antidote.zsh \
  /usr/local/opt/antidote/share/antidote/antidote.zsh \
  /usr/local/share/antidote/antidote.zsh \
  ${ZDOTDIR:-$HOME}/.antidote/antidote.zsh; do
  [[ -f $_antidote_path ]] && { source "$_antidote_path"; break; }
done
unset _antidote_path

# zsh-vi-mode defers init to first prompt and overwrites earlier bindkeys.
# Re-bind autosuggestions / history-substring-search inside its post-init hook.
function zvm_after_init() {
  bindkey '^[[A' history-substring-search-up    2>/dev/null
  bindkey '^[[B' history-substring-search-down  2>/dev/null
  bindkey '^P'   history-substring-search-up    2>/dev/null
  bindkey '^N'   history-substring-search-down  2>/dev/null
  bindkey '^ '   autosuggest-accept             2>/dev/null
}

# Static plugin loading — recompile bundle when source list changes
zsh_plugins=${ZDOTDIR:-$HOME}/.zsh_plugins.txt
[[ -f $zsh_plugins ]] || touch $zsh_plugins
if [[ ! ${zsh_plugins}.zsh -nt $zsh_plugins ]]; then
  antidote bundle <$zsh_plugins >${zsh_plugins}.zsh
fi
source ${zsh_plugins}.zsh
unset zsh_plugins

# NOTE: per-tool shell init (starship, zoxide, mcfly, fzf, direnv, thefuck …)
# is owned by `gt setup <tool>` — each writes its own marker block in this rc
# file so the plugin manager stays decoupled from individual tool integrations.
# --- end antidote bootstrap ---
EOF
}

# Returns the default curated list of Zsh plugins for ~/.zsh_plugins.txt.
#
# Architecture: zephyr (foundation) + OMZ (tool-specific only) + curated extras.
# zephyr is the antidote author's own modular replacement for OMZ libs — faster
# startup, no legacy cruft. We still pull in OMZ for plugins zephyr intentionally
# omits (docker, git aliases, sudo widget, extract, command-not-found …).
#
# Order is significant — DO NOT reorder without understanding why:
#   1. fpath-only plugins (zsh-completions) BEFORE any plugin that runs compinit
#   2. zephyr foundation modules before zephyr's `completion` module
#   3. zephyr/completion runs compinit, so it must come before fzf-tab
#   4. zsh-vi-mode AFTER zephyr/editor so its bindings win
#   5. fzf-tab AFTER compinit but BEFORE plugins that wrap the Tab widget
#      (autosuggestions, fast-syntax-highlighting)
#   6. fast-syntax-highlighting BEFORE zsh-history-substring-search
#   7. getantidote/use-omz required for OMZ subplugins that depend on OMZ libs
template_antidote_plugins() {
  cat <<EOF
# ─── Completions library (fpath only — must precede any compinit) ───
zsh-users/zsh-completions path:src kind:fpath

# ─── Zephyr foundation (modular replacement for OMZ libs / supercharge) ───
mattmc3/zephyr path:plugins/environment
mattmc3/zephyr path:plugins/editor
mattmc3/zephyr path:plugins/history
mattmc3/zephyr path:plugins/directory
mattmc3/zephyr path:plugins/utility
mattmc3/zephyr path:plugins/completion

# ─── Vi mode (after zephyr/editor so its bindings win; zvm_after_init re-binds) ───
jeffreytse/zsh-vi-mode

# ─── fzf-powered tab completion (after compinit, before widget wrappers) ───
Aloxaf/fzf-tab

# ─── Editing ergonomics ───
hlissner/zsh-autopair
olets/zsh-abbr

# ─── Oh-my-zsh atomic plugins (use-omz wires up OMZ runtime libs) ───
getantidote/use-omz
ohmyzsh/ohmyzsh path:plugins/extract
ohmyzsh/ohmyzsh path:plugins/sudo
ohmyzsh/ohmyzsh path:plugins/colored-man-pages
ohmyzsh/ohmyzsh path:plugins/command-not-found
ohmyzsh/ohmyzsh path:plugins/git
ohmyzsh/ohmyzsh path:plugins/docker
ohmyzsh/ohmyzsh path:plugins/docker-compose

# ─── fzf + git workflow (gd, glo, gss, gcf, ga, gi …) ───
wfxr/forgit

# ─── Reminds you of existing aliases / functions ───
MichaelAquilina/zsh-you-should-use

# ─── Autosuggestions (fish-style, must be before syntax highlighting) ───
zsh-users/zsh-autosuggestions

# ─── Syntax highlighting (must be before history-substring-search) ───
zdharma-continuum/fast-syntax-highlighting

# ─── History search (MUST be the last widget-binding plugin) ───
zsh-users/zsh-history-substring-search

# ─── OS Scripts Local Integrations (Cached Shell Init) ───
ttungbmt/os-scripts path:plugins/zsh-zoxide
ttungbmt/os-scripts path:plugins/zsh-mcfly
ttungbmt/os-scripts path:plugins/zsh-fzf
ttungbmt/os-scripts path:plugins/zsh-direnv
ttungbmt/os-scripts path:plugins/zsh-thefuck
ttungbmt/os-scripts path:plugins/zsh-starship
EOF
}

# src/lib/templates/starship.sh

# Returns a curated default ~/.config/starship.toml tuned for devops workflows.
# Requires a Nerd Font for glyphs. Disable individual modules by setting
# `disabled = true` or remove them from the `format` string.
template_starship_config() {
  cat <<'EOF'
# ~/.config/starship.toml — managed by os-scripts
# Curated default for devops workflows. Requires a Nerd Font:
#   https://www.nerdfonts.com
# Browse alternative presets:  starship preset list
# Apply a preset:              starship preset tokyo-night -o ~/.config/starship.toml
# Full reference:              https://starship.rs/config/

"$schema" = "https://starship.rs/config-schema.json"

# Two-line layout: context on top, prompt char on a fresh line
format = """
$os\
$username\
$directory\
$git_branch\
$git_status\
$git_state\
$kubernetes\
$aws\
$gcloud\
$azure\
$docker_context\
$package\
$nodejs\
$python\
$golang\
$rust\
$java\
$terraform\
$cmd_duration\
$line_break\
$character"""

add_newline = true
command_timeout = 1500
scan_timeout = 30

[character]
success_symbol = "[❯](bold green)"
error_symbol   = "[❯](bold red)"
vimcmd_symbol  = "[❮](bold green)"

[directory]
style             = "bold cyan"
truncation_length = 4
truncate_to_repo  = true
read_only         = " "
read_only_style   = "red"
home_symbol       = "~"

[git_branch]
symbol = " "
style  = "bold purple"
format = "on [$symbol$branch]($style) "

[git_status]
style      = "bold red"
ahead      = "⇡${count}"
behind     = "⇣${count}"
diverged   = "⇕⇡${ahead_count}⇣${behind_count}"
conflicted = "="
modified   = "!"
staged     = "+"
untracked  = "?"
deleted    = "✘"
stashed    = "$"

[kubernetes]
disabled         = false
symbol           = "☸ "
style            = "bold blue"
format           = '[$symbol$context( \($namespace\))]($style) '
detect_files     = ['k8s', 'kubeconfig', 'Chart.yaml']
detect_extensions = ['yaml', 'yml']
contexts = [
  { context_pattern = ".*[pP][rR][oO][dD].*",  style = "bold red", context_alias = "PROD ⚠" },
  { context_pattern = ".*[sS][tT][aA][gG].*",  style = "bold yellow" },
]

[docker_context]
symbol          = " "
style           = "bold blue"
only_with_files = true

[aws]
symbol = "☁️  "
style  = "bold yellow"
format = '[$symbol($profile )(\($region\) )]($style)'

[gcloud]
symbol = "☁️  "
style  = "bold blue"
format = '[$symbol$account(@$domain)(\($region\))]($style) '

[azure]
symbol = "☁️  "
style  = "bold blue"
format = '[$symbol($subscription)]($style) '

[package]
symbol = "📦 "
style  = "208"

[nodejs]
symbol = " "
style  = "bold green"

[python]
symbol = " "
style  = "bold yellow"

[golang]
symbol = " "
style  = "bold cyan"

[rust]
symbol = " "
style  = "bold red"

[java]
symbol = " "
style  = "bold red"

[terraform]
symbol = "💠 "
style  = "bold purple"

[os]
disabled = false
[os.symbols]
Alpine   = " "
Arch     = " "
CentOS   = " "
Debian   = " "
Fedora   = " "
Linux    = " "
Macos    = " "
NixOS    = " "
Raspbian = " "
Ubuntu   = " "
Windows  = " "

[cmd_duration]
min_time = 2000
format   = "took [$duration](bold yellow) "

[username]
show_always = false
style_user  = "bold yellow"
style_root  = "bold red"
format      = "[$user]($style)@"

[hostname]
ssh_only = true
style    = "bold green"
format   = "[$hostname]($style) "
EOF
}

# src/lib/version_helpers.sh

# Default local version probe for tools that don't define their own fetch_local_version.
# Tries `version --client --short` then falls back to `--version`.
_default_fetch_local_version() {
  local target="$1"
  "$target" version --client --short 2>/dev/null | awk '{print $2}' \
    || "$target" --version 2>/dev/null | awk '{print $3}'
}

# Get the local version of a tool (without 'v' prefix)
# Usage: get_local_version "tool_name"
# Returns: version string (e.g., "0.50.18") or empty if not installed/error
get_local_version() {
  local tool="$1"
  local fn_name
  fn_name="$(echo "$tool" | tr '-' '_')_fetch_local_version"
  local target="/usr/local/bin/$tool"

  if [ ! -f "$target" ]; then
    target=$(command -v "$tool" 2>/dev/null)
  fi

  if [ -z "$target" ] || [ ! -f "$target" ]; then
    echo ""
    return
  fi

  local ver=""
  if type "$fn_name" >/dev/null 2>&1; then
    ver=$("$fn_name" "$target")
  else
    ver=$(_default_fetch_local_version "$target")
  fi

  # Strip leading v
  if [[ "$ver" == v* ]]; then
    ver="${ver:1}"
  fi

  echo "$ver"
}

# Get the remote version of a tool (without 'v' prefix)
# Usage: get_remote_version "tool_name"
# Returns: version string (e.g., "0.50.18") or empty if error
get_remote_version() {
  local tool="$1"
  local tool_upper
  tool_upper=$(echo "$tool" | tr '[:lower:]' '[:upper:]' | tr '-' '_')

  local fn_name
  fn_name="$(echo "$tool" | tr '-' '_')_fetch_remote_version"
  local repo_var="${tool_upper}_GITHUB_REPO"

  local ver=""

  if type "$fn_name" >/dev/null 2>&1; then
    ver=$("$fn_name")
  else
    # Use indirect reference to get the repo value
    local repo_val="${!repo_var}"
    if [ -n "$repo_val" ]; then
      local api_opts=(-s "https://api.github.com/repos/${repo_val}/releases/latest")
      if [[ -n "$GITHUB_TOKEN" ]]; then
        api_opts=(-s -H "Authorization: Bearer $GITHUB_TOKEN" "https://api.github.com/repos/${repo_val}/releases/latest")
      fi
      ver=$(curl "${api_opts[@]}" | grep '"tag_name":' | sed -E 's/.*"v?([^"]+)".*/\1/')
    fi
  fi

  # Strip leading v just in case
  if [[ "$ver" == v* ]]; then
    ver="${ver:1}"
  fi

  echo "$ver"
}

# :command.command_functions

# :command.function
gt_install_multi_command() {
  # src/commands/install/multi.sh
  force=${args[--force]}
  eval "raw_tools=(${args['tools']})"

  # Process both space-separated and comma-separated inputs
  tools=()
  for t in "${raw_tools[@]}"; do
    IFS=',' read -ra split_tools <<< "$t"
    for st in "${split_tools[@]}"; do
      if [[ -n "$st" ]]; then
        tools+=("$st")
      fi
    done
  done

  for tool in "${tools[@]}"; do
    # Check if the tool exists in our registry
    registry_file="src/lib/registry/${tool}.sh"

    if [ -f "$registry_file" ] || declare -F "gt_install_${tool}_command" > /dev/null; then
      echo "==========================================="
      echo "Installing: $(cyan_bold "$tool")"
      echo "==========================================="

      # We call the internal bashly generated command function for that tool if it exists
      if declare -F "gt_install_${tool}_command" > /dev/null; then
        # Setup arguments exactly as the command expects
        # We clear existing args first
        local orig_args
        orig_args=$(declare -p args)

        # Prepare args for the specific tool command
        args['--version']="latest"
        if [[ -n "$force" ]]; then
          args['--force']=1
        fi

        # Call the internal function in a subshell to prevent 'exit' from killing the loop
        ( gt_install_${tool}_command ) || true

        # Restore args
        eval "$orig_args"
      else
        # Fallback to generic if command function doesn't exist but registry does
        export TARGET_TOOL="$tool"
        ( run_generic_install "$tool" "latest" "$force" ) || true
      fi
      echo ""
    else
      echo "$(red "Error:") Tool '$(cyan_bold "$tool")' is not supported or not found."
      echo ""
    fi
  done

}

# :command.function
gt_install_kustomize_command() {
  # src/commands/install/kustomize.sh
  export TARGET_TOOL="kustomize"
  run_generic_install "kustomize" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_k9s_command() {
  # src/commands/install/k9s.sh
  export TARGET_TOOL="k9s"
  run_generic_install "k9s" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_kubectl_command() {
  # src/commands/install/kubectl.sh
  export TARGET_TOOL="kubectl"
  run_generic_install "kubectl" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_fastfetch_command() {
  # src/commands/install/fastfetch.sh
  export TARGET_TOOL="fastfetch"
  run_generic_install "fastfetch" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_direnv_command() {
  # src/commands/install/direnv.sh
  export TARGET_TOOL="direnv"
  run_generic_install "direnv" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_btop_command() {
  # src/commands/install/btop.sh
  export TARGET_TOOL="btop"
  run_generic_install "btop" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_zsh_command() {
  # src/commands/install/zsh.sh
  version=${args[--version]}
  force=${args[--force]}
  name="zsh"

  # --- Step 1: Guard against overwrite ---
  if command -v zsh >/dev/null 2>&1 && [ -z "$force" ]; then
    echo "$(red Error:) ${name} is already installed at $(command -v zsh)."
    echo "Use $(bold --force) (or $(bold -f)) to overwrite/reinstall."
    exit 1
  fi

  echo "Installing $(cyan_bold "$name") via package manager..."

  # --- Step 2: Install via package manager ---
  if install_package "$name"; then
    echo "$(green_bold ✓) $name installed successfully."
  else
    echo "$(red ✗ Failed to install $name.)"
    exit 1
  fi

}

# :command.function
gt_install_antidote_command() {
  # src/commands/install/antidote.sh
  version=${args[--version]}
  force=${args[--force]}
  name="antidote"
  repo_url="https://github.com/mattmc3/antidote.git"
  target="/usr/local/share/antidote"

  # --- Step 1: Guard against overwrite ---
  if [ -d "$target" ] && [ -z "$force" ]; then
    echo "$(red Error:) ${name} is already installed at ${target}."
    echo "Use $(bold --force) (or $(bold -f)) to update/overwrite."
    exit 1
  fi

  echo "Installing $(cyan_bold "$name")..."

  # --- Step 2: Resolve version ---
  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "mattmc3/antidote")
  fi

  # --- Step 3: Install via git clone ---
  if install_git_repo "$repo_url" "$target" "$version"; then
    echo "$(green_bold ✓) $name installed successfully at $target."
    echo "To use it, add this to your ~/.zshrc:"
    echo "$(yellow "source ${target}/antidote.zsh")"
  else
    echo "$(red ✗ Failed to install $name.)"
    exit 1
  fi

}

# :command.function
gt_install_thefuck_command() {
  # src/commands/install/thefuck.sh
  export TARGET_TOOL="thefuck"
  run_generic_install "thefuck" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_argocd_command() {
  # src/commands/install/argocd.sh
  export TARGET_TOOL="argocd"
  run_generic_install "argocd" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_velero_command() {
  # src/commands/install/velero.sh
  export TARGET_TOOL="velero"
  run_generic_install "velero" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_kubeconform_command() {
  # src/commands/install/kubeconform.sh
  export TARGET_TOOL="kubeconform"
  run_generic_install "kubeconform" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_krew_command() {
  # src/commands/install/krew.sh
  export TARGET_TOOL="krew"
  run_generic_install "krew" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_kubent_command() {
  # src/commands/install/kubent.sh
  export TARGET_TOOL="kubent"
  run_generic_install "kubent" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_sops_command() {
  # src/commands/install/sops.sh
  export TARGET_TOOL="sops"
  run_generic_install "sops" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_age_command() {
  # src/commands/install/age.sh
  export TARGET_TOOL="age"
  run_generic_install "age" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_trivy_command() {
  # src/commands/install/trivy.sh
  export TARGET_TOOL="trivy"
  run_generic_install "trivy" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_kubens_command() {
  # src/commands/install/kubens.sh
  export TARGET_TOOL="kubens"
  run_generic_install "kubens" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_kubecolor_command() {
  # src/commands/install/kubecolor.sh
  export TARGET_TOOL="kubecolor"
  run_generic_install "kubecolor" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_popeye_command() {
  # src/commands/install/popeye.sh
  export TARGET_TOOL="popeye"
  run_generic_install "popeye" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_kube_linter_command() {
  # src/commands/install/kube-linter.sh
  export TARGET_TOOL="kube-linter"
  run_generic_install "kube-linter" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_kyverno_command() {
  # src/commands/install/kyverno.sh
  export TARGET_TOOL="kyverno"
  run_generic_install "kyverno" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_conftest_command() {
  # src/commands/install/conftest.sh
  export TARGET_TOOL="conftest"
  run_generic_install "conftest" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_vault_command() {
  # src/commands/install/vault.sh
  export TARGET_TOOL="vault"
  run_generic_install "vault" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_kubeseal_command() {
  # src/commands/install/kubeseal.sh
  export TARGET_TOOL="kubeseal"
  run_generic_install "kubeseal" "${args[--version]}" "${args[--force]}"

}

# :command.function
gt_install_zoxide_command() {
  # src/commands/install/zoxide.sh
  version=${args[--version]}
  force=${args[--force]}
  name="zoxide"
  target="/usr/local/bin/zoxide"

  # --- Step 1: Guard against overwrite ---
  guard_existing "$name" "$target" "$force"

  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  # --- Step 2: Resolve version ---
  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "ajeetdsouza/zoxide")
  fi

  # Strip 'v' prefix if it exists
  if [[ "$version" == v* ]]; then
    version="${version:1}"
  fi

  # --- Step 3: Detect platform ---
  detect_platform

  # Map OS and Architecture
  os="unknown-linux-musl"
  if [[ "$DETECT_OS" == "darwin" ]]; then
    os="apple-darwin"
  fi

  arch="x86_64"
  if [[ "$DETECT_ARCH" == "arm64" ]]; then
    arch="aarch64"
  fi

  # --- Step 4: Build download URL ---
  download_url="https://github.com/ajeetdsouza/zoxide/releases/download/v${version}/zoxide-${version}-${arch}-${os}.tar.gz"

  # --- Step 5: Download & extract ---
  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/zoxide.tar.gz"
  tar -xzf "$temp_dir/zoxide.tar.gz" -C "$temp_dir" zoxide

  # --- Step 6: Install binary ---
  if [ -f "$temp_dir/zoxide" ]; then
    install_binary "$temp_dir/zoxide" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "v${version}")"
  else
    echo "$(red ✗ Failed to install $name.)"
    rm -rf "$temp_dir"
    exit 1
  fi

}

# :command.function
gt_install_eza_command() {
  # src/commands/install/eza.sh
  version=${args[--version]}
  force=${args[--force]}
  name="eza"
  target="/usr/local/bin/eza"

  # --- Step 1: Guard against overwrite ---
  guard_existing "$name" "$target" "$force"

  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  # --- Step 2: Resolve version ---
  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "eza-community/eza")
  fi

  # Strip 'v' prefix for URL construction
  if [[ "$version" == v* ]]; then
    version="${version:1}"
  fi

  # --- Step 3: Detect platform ---
  detect_platform

  # Map architecture
  arch="x86_64"
  if [[ "$DETECT_ARCH" == "arm64" ]]; then
    arch="aarch64"
  fi

  # --- Step 4: Build download URL ---
  download_url="https://github.com/eza-community/eza/releases/download/v${version}/eza_${arch}-unknown-linux-gnu.tar.gz"

  # --- Step 5: Download & extract ---
  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/eza.tar.gz"
  tar -xzf "$temp_dir/eza.tar.gz" -C "$temp_dir" ./eza

  # --- Step 6: Install binary ---
  if [ -f "$temp_dir/eza" ]; then
    install_binary "$temp_dir/eza" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "v${version}")"
  else
    echo "$(red ✗ Failed to install $name.)"
    rm -rf "$temp_dir"
    exit 1
  fi

}

# :command.function
gt_install_bat_command() {
  # src/commands/install/bat.sh
  version=${args[--version]}
  force=${args[--force]}
  name="bat"
  target="/usr/local/bin/bat"

  # --- Step 1: Guard against overwrite ---
  guard_existing "$name" "$target" "$force"

  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  # --- Step 2: Resolve version ---
  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "sharkdp/bat")
  fi

  # Strip 'v' prefix for URL construction
  if [[ "$version" == v* ]]; then
    version="${version:1}"
  fi

  # --- Step 3: Detect platform ---
  detect_platform

  # Map architecture
  arch="x86_64"
  if [[ "$DETECT_ARCH" == "arm64" ]]; then
    arch="aarch64"
  fi

  # Map OS
  os="unknown-linux-musl"
  if [[ "$DETECT_OS" == "darwin" ]]; then
    os="apple-darwin"
  fi

  # --- Step 4: Build download URL ---
  archive_name="bat-v${version}-${arch}-${os}"
  download_url="https://github.com/sharkdp/bat/releases/download/v${version}/${archive_name}.tar.gz"

  # --- Step 5: Download & extract ---
  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/bat.tar.gz"
  tar -xzf "$temp_dir/bat.tar.gz" -C "$temp_dir"

  # --- Step 6: Install binary ---
  if [ -f "$temp_dir/${archive_name}/bat" ]; then
    install_binary "$temp_dir/${archive_name}/bat" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "v${version}")"
  else
    echo "$(red ✗ Failed to install $name.)"
    rm -rf "$temp_dir"
    exit 1
  fi

}

# :command.function
gt_install_ripgrep_command() {
  # src/commands/install/ripgrep.sh
  version=${args[--version]}
  force=${args[--force]}
  name="ripgrep"
  target="/usr/local/bin/rg"

  # --- Step 1: Guard against overwrite ---
  guard_existing "$name" "$target" "$force"

  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  # --- Step 2: Resolve version ---
  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "BurntSushi/ripgrep")
  fi

  # Strip 'v' prefix if present (ripgrep tags have no v prefix)
  if [[ "$version" == v* ]]; then
    version="${version:1}"
  fi

  # --- Step 3: Detect platform ---
  detect_platform

  # Map architecture
  arch="x86_64"
  if [[ "$DETECT_ARCH" == "arm64" ]]; then
    arch="aarch64"
  fi

  # Map OS
  os="unknown-linux-musl"
  if [[ "$DETECT_OS" == "darwin" ]]; then
    os="apple-darwin"
  fi

  # --- Step 4: Build download URL ---
  archive_name="ripgrep-${version}-${arch}-${os}"
  download_url="https://github.com/BurntSushi/ripgrep/releases/download/${version}/${archive_name}.tar.gz"

  # --- Step 5: Download & extract ---
  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/ripgrep.tar.gz"
  tar -xzf "$temp_dir/ripgrep.tar.gz" -C "$temp_dir"

  # --- Step 6: Install binary ---
  if [ -f "$temp_dir/${archive_name}/rg" ]; then
    install_binary "$temp_dir/${archive_name}/rg" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "v${version}")"
  else
    echo "$(red ✗ Failed to install $name.)"
    rm -rf "$temp_dir"
    exit 1
  fi

}

# :command.function
gt_install_starship_command() {
  # src/commands/install/starship.sh
  version=${args[--version]}
  force=${args[--force]}
  name="starship"
  target="/usr/local/bin/starship"

  # --- Step 1: Guard against overwrite ---
  guard_existing "$name" "$target" "$force"

  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  # --- Step 2: Resolve version ---
  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "starship/starship")
  fi

  # Strip 'v' prefix for URL construction
  if [[ "$version" == v* ]]; then
    version="${version:1}"
  fi

  # --- Step 3: Detect platform ---
  detect_platform

  # Map architecture (starship uses x86_64/aarch64)
  arch="x86_64"
  if [[ "$DETECT_ARCH" == "arm64" ]]; then
    arch="aarch64"
  fi

  # Map OS
  os="unknown-linux-musl"
  if [[ "$DETECT_OS" == "darwin" ]]; then
    os="apple-darwin"
  fi

  # --- Step 4: Build download URL ---
  download_url="https://github.com/starship/starship/releases/download/v${version}/starship-${arch}-${os}.tar.gz"

  # --- Step 5: Download & extract ---
  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/starship.tar.gz"
  tar -xzf "$temp_dir/starship.tar.gz" -C "$temp_dir" starship

  # --- Step 6: Install binary ---
  if [ -f "$temp_dir/starship" ]; then
    install_binary "$temp_dir/starship" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "v${version}")"
  else
    echo "$(red ✗ Failed to install $name.)"
    rm -rf "$temp_dir"
    exit 1
  fi

}

# :command.function
gt_install_fzf_command() {
  # src/commands/install/fzf.sh
  version=${args[--version]}
  force=${args[--force]}
  name="fzf"
  target="/usr/local/bin/fzf"

  # --- Step 1: Guard against overwrite ---
  guard_existing "$name" "$target" "$force"

  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  # --- Step 2: Resolve version ---
  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "junegunn/fzf")
  fi

  # Strip 'v' prefix for URL construction
  if [[ "$version" == v* ]]; then
    version="${version:1}"
  fi

  # --- Step 3: Detect platform ---
  detect_platform

  # fzf uses linux/darwin for OS and amd64/arm64 for arch
  os="linux"
  if [[ "$DETECT_OS" == "darwin" ]]; then
    os="darwin"
  fi

  arch="$DETECT_ARCH"

  # --- Step 4: Build download URL ---
  download_url="https://github.com/junegunn/fzf/releases/download/v${version}/fzf-${version}-${os}_${arch}.tar.gz"

  # --- Step 5: Download & extract ---
  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/fzf.tar.gz"
  tar -xzf "$temp_dir/fzf.tar.gz" -C "$temp_dir" fzf

  # --- Step 6: Install binary ---
  if [ -f "$temp_dir/fzf" ]; then
    install_binary "$temp_dir/fzf" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "v${version}")"
  else
    echo "$(red ✗ Failed to install $name.)"
    rm -rf "$temp_dir"
    exit 1
  fi

}

# :command.function
gt_install_fd_command() {
  # src/commands/install/fd.sh
  version=${args[--version]}
  force=${args[--force]}
  name="fd"
  target="/usr/local/bin/fd"

  # --- Step 1: Guard against overwrite ---
  guard_existing "$name" "$target" "$force"

  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  # --- Step 2: Resolve version ---
  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "sharkdp/fd")
  fi

  # Strip 'v' prefix for URL construction
  if [[ "$version" == v* ]]; then
    version="${version:1}"
  fi

  # --- Step 3: Detect platform ---
  detect_platform

  # fd uses x86_64/aarch64 arch naming
  arch="x86_64"
  if [[ "$DETECT_ARCH" == "arm64" ]]; then
    arch="aarch64"
  fi

  # --- Step 4: Build download URL ---
  # Archive: fd-v{ver}-x86_64-unknown-linux-musl.tar.gz
  # Binary inside: fd-v{ver}-x86_64-unknown-linux-musl/fd
  download_url="https://github.com/sharkdp/fd/releases/download/v${version}/fd-v${version}-${arch}-unknown-linux-musl.tar.gz"
  binary_path="fd-v${version}-${arch}-unknown-linux-musl/fd"

  # --- Step 5: Download & extract ---
  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/fd.tar.gz"
  tar -xzf "$temp_dir/fd.tar.gz" -C "$temp_dir" "$binary_path"

  # --- Step 6: Install binary ---
  if [ -f "$temp_dir/$binary_path" ]; then
    install_binary "$temp_dir/$binary_path" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "v${version}")"
  else
    echo "$(red ✗ Failed to install $name.)"
    rm -rf "$temp_dir"
    exit 1
  fi

}

# :command.function
gt_install_mcfly_command() {
  # src/commands/install/mcfly.sh
  version=${args[--version]}
  force=${args[--force]}
  name="mcfly"
  target="/usr/local/bin/mcfly"

  # --- Step 1: Guard against overwrite ---
  guard_existing "$name" "$target" "$force"

  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  # --- Step 2: Resolve version ---
  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "cantino/mcfly")
  fi

  # Strip 'v' prefix for URL construction
  if [[ "$version" == v* ]]; then
    version="${version:1}"
  fi

  # --- Step 3: Detect platform ---
  detect_platform

  # mcfly uses x86_64/aarch64 arch naming
  arch="x86_64"
  if [[ "$DETECT_ARCH" == "arm64" ]]; then
    arch="aarch64"
  fi

  # --- Step 4: Build download URL ---
  # Archive: mcfly-v{ver}-x86_64-unknown-linux-musl.tar.gz
  # Binary inside: bare mcfly
  download_url="https://github.com/cantino/mcfly/releases/download/v${version}/mcfly-v${version}-${arch}-unknown-linux-musl.tar.gz"

  # --- Step 5: Download & extract ---
  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/mcfly.tar.gz"
  tar -xzf "$temp_dir/mcfly.tar.gz" -C "$temp_dir" mcfly

  # --- Step 6: Install binary ---
  if [ -f "$temp_dir/mcfly" ]; then
    install_binary "$temp_dir/mcfly" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "v${version}")"
  else
    echo "$(red ✗ Failed to install $name.)"
    rm -rf "$temp_dir"
    exit 1
  fi

}

# :command.function
gt_install_infisical_command() {
  # src/commands/install/infisical.sh
  version=${args[--version]}
  force=${args[--force]}
  name="infisical"
  target="/usr/local/bin/infisical"

  guard_existing "$name" "$target" "$force"
  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "Infisical/cli")
  fi
  if [[ "$version" == v* ]]; then
    version="${version:1}"
  fi

  detect_platform
  arch="$DETECT_ARCH"

  # Asset: cli_{ver}_linux_amd64.tar.gz — bare infisical inside
  download_url="https://github.com/Infisical/cli/releases/download/v${version}/cli_${version}_linux_${arch}.tar.gz"

  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/infisical.tar.gz"
  tar -xzf "$temp_dir/infisical.tar.gz" -C "$temp_dir" infisical

  if [ -f "$temp_dir/infisical" ]; then
    install_binary "$temp_dir/infisical" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "v${version}")"
  else
    rm -rf "$temp_dir"; echo "$(red ✗ Failed to install $name.)"; exit 1
  fi

}

# :command.function
gt_install_ksops_command() {
  # src/commands/install/ksops.sh
  version=${args[--version]}
  force=${args[--force]}
  name="ksops"
  target="/usr/local/bin/ksops"

  guard_existing "$name" "$target" "$force"
  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "viaduct-ai/kustomize-sops")
  fi
  if [[ "$version" == v* ]]; then
    version="${version:1}"
  fi

  detect_platform
  arch="x86_64"
  if [[ "$DETECT_ARCH" == "arm64" ]]; then arch="arm64"; fi
  os="Linux"
  if [[ "$DETECT_OS" == "darwin" ]]; then os="Darwin"; fi

  # Asset: ksops_{ver}_Linux_x86_64.tar.gz — bare ksops inside
  download_url="https://github.com/viaduct-ai/kustomize-sops/releases/download/v${version}/ksops_${version}_${os}_${arch}.tar.gz"

  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/ksops.tar.gz"
  tar -xzf "$temp_dir/ksops.tar.gz" -C "$temp_dir" ksops

  if [ -f "$temp_dir/ksops" ]; then
    install_binary "$temp_dir/ksops" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "v${version}")"
  else
    rm -rf "$temp_dir"; echo "$(red ✗ Failed to install $name.)"; exit 1
  fi

}

# :command.function
gt_install_skopeo_command() {
  # src/commands/install/skopeo.sh
  force=${args[--force]}
  name="skopeo"

  if command -v skopeo >/dev/null 2>&1 && [ -z "$force" ]; then
    echo "$(red Error:) ${name} is already installed at $(command -v skopeo)."
    echo "Use $(bold --force) (or $(bold -f)) to overwrite/reinstall."
    exit 1
  fi

  echo "Installing $(cyan_bold "$name") via package manager..."

  if install_package "$name"; then
    echo "$(green_bold ✓) $name installed successfully."
  else
    echo "$(red ✗ Failed to install $name.)"; exit 1
  fi

}

# :command.function
gt_install_helmfile_command() {
  # src/commands/install/helmfile.sh
  version=${args[--version]}
  force=${args[--force]}
  name="helmfile"
  target="/usr/local/bin/helmfile"

  guard_existing "$name" "$target" "$force"
  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "helmfile/helmfile")
  fi
  if [[ "$version" == v* ]]; then
    version="${version:1}"
  fi

  detect_platform
  arch="$DETECT_ARCH"

  # Asset: helmfile_{ver}_linux_amd64.tar.gz — bare helmfile inside
  download_url="https://github.com/helmfile/helmfile/releases/download/v${version}/helmfile_${version}_linux_${arch}.tar.gz"

  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/helmfile.tar.gz"
  tar -xzf "$temp_dir/helmfile.tar.gz" -C "$temp_dir" helmfile

  if [ -f "$temp_dir/helmfile" ]; then
    install_binary "$temp_dir/helmfile" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "v${version}")"
  else
    rm -rf "$temp_dir"; echo "$(red ✗ Failed to install $name.)"; exit 1
  fi

}

# :command.function
gt_install_helm_command() {
  # src/commands/install/helm.sh
  version=${args[--version]}
  force=${args[--force]}
  name="helm"
  target="/usr/local/bin/helm"

  guard_existing "$name" "$target" "$force"
  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "helm/helm")
  fi
  # helm URL always includes v prefix
  if [[ "$version" != v* ]]; then
    version="v${version}"
  fi

  detect_platform
  arch="$DETECT_ARCH"
  os="$DETECT_OS"

  # Asset: helm-v{ver}-linux-amd64.tar.gz — binary at linux-amd64/helm
  download_url="https://get.helm.sh/helm-${version}-${os}-${arch}.tar.gz"

  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/helm.tar.gz"
  tar -xzf "$temp_dir/helm.tar.gz" -C "$temp_dir" "${os}-${arch}/helm"

  binary_path="${os}-${arch}/helm"
  if [ -f "$temp_dir/$binary_path" ]; then
    install_binary "$temp_dir/$binary_path" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "${version}")"
  else
    rm -rf "$temp_dir"; echo "$(red ✗ Failed to install $name.)"; exit 1
  fi

}

# :command.function
gt_install_chezmoi_command() {
  # src/commands/install/chezmoi.sh
  version=${args[--version]}
  force=${args[--force]}
  name="chezmoi"
  target="/usr/local/bin/chezmoi"

  guard_existing "$name" "$target" "$force"
  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "twpayne/chezmoi")
  fi
  if [[ "$version" == v* ]]; then
    version="${version:1}"
  fi

  detect_platform
  arch="$DETECT_ARCH"
  os="$DETECT_OS"

  # Asset: chezmoi_{ver}_linux_amd64.tar.gz — bare chezmoi inside
  download_url="https://github.com/twpayne/chezmoi/releases/download/v${version}/chezmoi_${version}_${os}_${arch}.tar.gz"

  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/chezmoi.tar.gz"
  tar -xzf "$temp_dir/chezmoi.tar.gz" -C "$temp_dir" chezmoi

  if [ -f "$temp_dir/chezmoi" ]; then
    install_binary "$temp_dir/chezmoi" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "v${version}")"
  else
    rm -rf "$temp_dir"; echo "$(red ✗ Failed to install $name.)"; exit 1
  fi

}

# :command.function
gt_install_tmux_command() {
  # src/commands/install/tmux.sh
  force=${args[--force]}
  name="tmux"

  if command -v tmux >/dev/null 2>&1 && [ -z "$force" ]; then
    echo "$(red Error:) ${name} is already installed at $(command -v tmux)."
    echo "Use $(bold --force) (or $(bold -f)) to overwrite/reinstall."
    exit 1
  fi

  echo "Installing $(cyan_bold "$name") via package manager..."

  if install_package "$name"; then
    echo "$(green_bold ✓) $name installed successfully."
  else
    echo "$(red ✗ Failed to install $name.)"; exit 1
  fi

}

# :command.function
gt_install_rsync_command() {
  # src/commands/install/rsync.sh
  force=${args[--force]}
  name="rsync"

  if command -v rsync >/dev/null 2>&1 && [ -z "$force" ]; then
    echo "$(red Error:) ${name} is already installed at $(command -v rsync)."
    echo "Use $(bold --force) (or $(bold -f)) to overwrite/reinstall."
    exit 1
  fi

  echo "Installing $(cyan_bold "$name") via package manager..."

  if install_package "$name"; then
    echo "$(green_bold ✓) $name installed successfully."
  else
    echo "$(red ✗ Failed to install $name.)"; exit 1
  fi

}

# :command.function
gt_install_trino_command() {
  # src/commands/install/trino.sh
  version=${args[--version]}
  force=${args[--force]}
  name="trino"
  target="/usr/local/bin/trino"

  guard_existing "$name" "$target" "$force"
  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "trinodb/trino")
  fi
  # trino tags have no v prefix (e.g., 480)
  version="${version#v}"

  # Asset: trino-cli-{ver}-executable.jar (self-executable jar)
  download_url="https://repo1.maven.org/maven2/io/trino/trino-cli/${version}/trino-cli-${version}-executable.jar"

  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/trino"

  if [ -f "$temp_dir/trino" ]; then
    install_binary "$temp_dir/trino" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "${version}")"
  else
    rm -rf "$temp_dir"; echo "$(red ✗ Failed to install $name.)"; exit 1
  fi

}

# :command.function
gt_install_nerdctl_command() {
  # src/commands/install/nerdctl.sh
  version=${args[--version]}
  force=${args[--force]}
  name="nerdctl"
  target="/usr/local/bin/nerdctl"

  guard_existing "$name" "$target" "$force"
  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "containerd/nerdctl")
  fi
  if [[ "$version" == v* ]]; then
    version="${version:1}"
  fi

  detect_platform
  arch="$DETECT_ARCH"

  # Asset: nerdctl-{ver}-linux-amd64.tar.gz — bare nerdctl inside
  download_url="https://github.com/containerd/nerdctl/releases/download/v${version}/nerdctl-${version}-linux-${arch}.tar.gz"

  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/nerdctl.tar.gz"
  tar -xzf "$temp_dir/nerdctl.tar.gz" -C "$temp_dir" nerdctl

  if [ -f "$temp_dir/nerdctl" ]; then
    install_binary "$temp_dir/nerdctl" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "v${version}")"
  else
    rm -rf "$temp_dir"; echo "$(red ✗ Failed to install $name.)"; exit 1
  fi

}

# :command.function
gt_install_viddy_command() {
  # src/commands/install/viddy.sh
  version=${args[--version]}
  force=${args[--force]}
  name="viddy"
  target="/usr/local/bin/viddy"

  guard_existing "$name" "$target" "$force"
  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "sachaos/viddy")
  fi
  # viddy URL includes v prefix
  if [[ "$version" != v* ]]; then
    version="v${version}"
  fi

  detect_platform
  # viddy uses x86_64/arm64
  arch="x86_64"
  if [[ "$DETECT_ARCH" == "arm64" ]]; then arch="arm64"; fi
  os="linux"
  if [[ "$DETECT_OS" == "darwin" ]]; then os="darwin"; fi

  # Asset: viddy-v{ver}-linux-x86_64.tar.gz — bare viddy inside
  download_url="https://github.com/sachaos/viddy/releases/download/${version}/viddy-${version}-${os}-${arch}.tar.gz"

  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/viddy.tar.gz"
  tar -xzf "$temp_dir/viddy.tar.gz" -C "$temp_dir" viddy

  if [ -f "$temp_dir/viddy" ]; then
    install_binary "$temp_dir/viddy" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "${version}")"
  else
    rm -rf "$temp_dir"; echo "$(red ✗ Failed to install $name.)"; exit 1
  fi

}

# :command.function
gt_install_hyperfine_command() {
  # src/commands/install/hyperfine.sh
  version=${args[--version]}
  force=${args[--force]}
  name="hyperfine"
  target="/usr/local/bin/hyperfine"

  guard_existing "$name" "$target" "$force"
  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "sharkdp/hyperfine")
  fi
  if [[ "$version" != v* ]]; then
    version="v${version}"
  fi

  detect_platform
  arch="x86_64"
  if [[ "$DETECT_ARCH" == "arm64" ]]; then arch="aarch64"; fi

  # Asset: hyperfine-v{ver}-x86_64-unknown-linux-musl.tar.gz
  # Binary nested: hyperfine-v{ver}-x86_64-.../hyperfine
  download_url="https://github.com/sharkdp/hyperfine/releases/download/${version}/hyperfine-${version}-${arch}-unknown-linux-musl.tar.gz"
  binary_path="hyperfine-${version}-${arch}-unknown-linux-musl/hyperfine"

  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/hyperfine.tar.gz"
  tar -xzf "$temp_dir/hyperfine.tar.gz" -C "$temp_dir" "$binary_path"

  if [ -f "$temp_dir/$binary_path" ]; then
    install_binary "$temp_dir/$binary_path" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "${version}")"
  else
    rm -rf "$temp_dir"; echo "$(red ✗ Failed to install $name.)"; exit 1
  fi

}

# :command.function
gt_install_jq_command() {
  # src/commands/install/jq.sh
  version=${args[--version]}
  force=${args[--force]}
  name="jq"
  target="/usr/local/bin/jq"

  guard_existing "$name" "$target" "$force"
  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "jqlang/jq")
  fi
  # jq tags are like 'jq-1.8.1' — strip prefix for display but keep for URL
  tag="$version"

  detect_platform
  arch="$DETECT_ARCH"
  os="$DETECT_OS"

  # Asset: bare binary jq-linux-amd64 (no archive)
  download_url="https://github.com/jqlang/jq/releases/download/${tag}/jq-${os}-${arch}"

  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/jq"

  if [ -f "$temp_dir/jq" ]; then
    install_binary "$temp_dir/jq" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "${tag}")"
  else
    rm -rf "$temp_dir"; echo "$(red ✗ Failed to install $name.)"; exit 1
  fi

}

# :command.function
gt_install_yq_command() {
  # src/commands/install/yq.sh
  version=${args[--version]}
  force=${args[--force]}
  name="yq"
  target="/usr/local/bin/yq"

  guard_existing "$name" "$target" "$force"
  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "mikefarah/yq")
  fi

  detect_platform
  arch="$DETECT_ARCH"
  os="$DETECT_OS"

  # Asset: bare binary yq_linux_amd64 (no archive)
  download_url="https://github.com/mikefarah/yq/releases/download/${version}/yq_${os}_${arch}"

  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/yq"

  if [ -f "$temp_dir/yq" ]; then
    install_binary "$temp_dir/yq" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "${version}")"
  else
    rm -rf "$temp_dir"; echo "$(red ✗ Failed to install $name.)"; exit 1
  fi

}

# :command.function
gt_install_gum_command() {
  # src/commands/install/gum.sh
  version=${args[--version]}
  force=${args[--force]}
  name="gum"
  target="/usr/local/bin/gum"

  guard_existing "$name" "$target" "$force"
  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "charmbracelet/gum")
  fi
  if [[ "$version" == v* ]]; then
    version="${version:1}"
  fi

  detect_platform
  # gum uses x86_64/arm64, Linux/Darwin capitalized
  arch="x86_64"
  if [[ "$DETECT_ARCH" == "arm64" ]]; then arch="arm64"; fi
  os="Linux"
  if [[ "$DETECT_OS" == "darwin" ]]; then os="Darwin"; fi

  # Asset: gum_{ver}_Linux_x86_64.tar.gz — nested gum_{ver}_Linux_x86_64/gum
  download_url="https://github.com/charmbracelet/gum/releases/download/v${version}/gum_${version}_${os}_${arch}.tar.gz"
  binary_path="gum_${version}_${os}_${arch}/gum"

  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/gum.tar.gz"
  tar -xzf "$temp_dir/gum.tar.gz" -C "$temp_dir" "$binary_path"

  if [ -f "$temp_dir/$binary_path" ]; then
    install_binary "$temp_dir/$binary_path" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "v${version}")"
  else
    rm -rf "$temp_dir"; echo "$(red ✗ Failed to install $name.)"; exit 1
  fi

}

# :command.function
gt_install_uv_command() {
  # src/commands/install/uv.sh
  version=${args[--version]}
  force=${args[--force]}
  name="uv"
  target="/usr/local/bin/uv"

  guard_existing "$name" "$target" "$force"
  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "astral-sh/uv")
  fi
  # uv tags have no v prefix (e.g., 0.11.8)
  version="${version#v}"

  detect_platform
  arch="x86_64"
  if [[ "$DETECT_ARCH" == "arm64" ]]; then arch="aarch64"; fi

  # Asset: uv-x86_64-unknown-linux-musl.tar.gz — nested uv-x86_64-.../uv
  download_url="https://github.com/astral-sh/uv/releases/download/${version}/uv-${arch}-unknown-linux-musl.tar.gz"
  binary_path="uv-${arch}-unknown-linux-musl/uv"

  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/uv.tar.gz"
  tar -xzf "$temp_dir/uv.tar.gz" -C "$temp_dir" "$binary_path"

  if [ -f "$temp_dir/$binary_path" ]; then
    install_binary "$temp_dir/$binary_path" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "${version}")"
  else
    rm -rf "$temp_dir"; echo "$(red ✗ Failed to install $name.)"; exit 1
  fi

}

# :command.function
gt_install_bashly_command() {
  # src/commands/install/bashly.sh
  version=${args[--version]}
  force=${args[--force]}
  name="bashly"

  if command -v bashly >/dev/null 2>&1 && [ -z "$force" ]; then
    echo "$(red Error:) ${name} is already installed at $(command -v bashly)."
    echo "Use $(bold --force) (or $(bold -f)) to overwrite."
    exit 1
  fi

  echo "Installing $(cyan_bold "$name") via gem..."

  gem_args=()
  if [[ "$version" != "latest" ]]; then
    # Strip leading v if any
    ver="${version#v}"
    gem_args=(-v "$ver")
  fi

  if sudo gem install bashly "${gem_args[@]}"; then
    echo "$(green_bold ✓) $name installed successfully."
  else
    echo "$(red ✗ Failed to install $name.)"
    exit 1
  fi

}

# :command.function
gt_install_gem_command() {
  # src/commands/install/gem.sh
  force=${args[--force]}
  name="gem"

  # Guard
  if command -v gem >/dev/null 2>&1 && [ -z "$force" ]; then
    echo "$(red Error:) ${name} is already installed at $(command -v gem)."
    echo "Use $(bold --force) (or $(bold -f)) to overwrite/reinstall."
    exit 1
  fi

  echo "Installing $(cyan_bold "$name") via package manager..."

  if command -v apt-get >/dev/null 2>&1; then
    pkg="ruby-dev"
  elif command -v dnf >/dev/null 2>&1 || command -v yum >/dev/null 2>&1; then
    pkg="ruby-devel"
  elif command -v pacman >/dev/null 2>&1; then
    pkg="ruby"
  elif command -v apk >/dev/null 2>&1; then
    pkg="ruby-dev"
  else
    echo "$(red ✗) Could not detect a supported package manager."
    exit 1
  fi

  if install_package "$pkg"; then
    echo "$(green_bold ✓) $name installed successfully."
  else
    echo "$(red ✗ Failed to install $name.)"
    exit 1
  fi

}

# :command.function
gt_install_claude_command() {
  # src/commands/install/claude.sh
  version=${args[--version]}
  force=${args[--force]}
  name="claude"

  if command -v claude >/dev/null 2>&1 && [ -z "$force" ]; then
    echo "$(red Error:) ${name} is already installed at $(command -v claude)."
    echo "Use $(bold --force) (or $(bold -f)) to overwrite."
    exit 1
  fi

  echo "Installing $(cyan_bold "$name") via npm..."

  if [[ "$version" != "latest" ]]; then
    ver="${version#v}"
    npm_pkg="@anthropic-ai/claude-code@$ver"
  else
    npm_pkg="@anthropic-ai/claude-code"
  fi

  if npm install -g "$npm_pkg"; then
    echo "$(green_bold ✓) $name installed successfully."
  else
    echo "$(red ✗ Failed to install $name.)"
    exit 1
  fi

}

# :command.function
gt_install_talisman_command() {
  # src/commands/install/talisman.sh
  version=${args[--version]}
  force=${args[--force]}
  name="talisman"
  target="/usr/local/bin/talisman"

  # --- Step 1: Guard against overwrite ---
  guard_existing "$name" "$target" "$force"

  echo "Installing $(cyan_bold "$name") ($(yellow "$version"))..."

  # --- Step 2: Resolve version ---
  if [[ "$version" == "latest" ]]; then
    version=$(github_latest_tag "thoughtworks/talisman")
  fi

  if [[ "$version" == v* ]]; then
    version="${version:1}"
  fi

  # --- Step 3: Detect platform ---
  detect_platform

  # --- Step 4: Build download URL ---
  download_url="https://github.com/thoughtworks/talisman/releases/download/v${version}/talisman_${DETECT_OS}_${DETECT_ARCH}"

  # --- Step 5: Download & Install ---
  temp_dir=$(mktemp -d)
  download_file "$download_url" "$temp_dir/$name"

  if [ -f "$temp_dir/$name" ]; then
    install_binary "$temp_dir/$name" "$target"
    rm -rf "$temp_dir"
    echo "$(green_bold ✓) $name installed successfully: $(bold "v${version}")"
  else
    echo "$(red ✗ Failed to install $name.)"
    rm -rf "$temp_dir"
    exit 1
  fi

}

# :command.function
gt_uninstall_kustomize_command() {
  # src/commands/uninstall/kustomize.sh
  run_generic_uninstall "kustomize"

}

# :command.function
gt_uninstall_k9s_command() {
  # src/commands/uninstall/k9s.sh
  run_generic_uninstall "k9s"

}

# :command.function
gt_uninstall_kubectl_command() {
  # src/commands/uninstall/kubectl.sh
  run_generic_uninstall "kubectl"

}

# :command.function
gt_uninstall_fastfetch_command() {
  # src/commands/uninstall/fastfetch.sh
  run_generic_uninstall "fastfetch"

}

# :command.function
gt_uninstall_direnv_command() {
  # src/commands/uninstall/direnv.sh
  run_generic_uninstall "direnv"

}

# :command.function
gt_uninstall_btop_command() {
  # src/commands/uninstall/btop.sh
  run_generic_uninstall "btop"

}

# :command.function
gt_uninstall_zsh_command() {
  # src/commands/uninstall/zsh.sh
  name="zsh"

  if ! command -v zsh >/dev/null 2>&1; then
    echo "$(yellow ${name} is not installed)."
    exit 0
  fi

  echo -n "Are you sure you want to uninstall $(cyan_bold "${name}") via package manager? [y/N] "
  read -r response
  if [[ ! "$response" =~ ^[Yy]$ ]]; then
    echo "Aborted."
    exit 0
  fi

  echo "Uninstalling $(cyan_bold "${name}")..."

  if remove_package "$name"; then
    echo "$(green_bold ✓) ${name} uninstalled successfully."
  else
    echo "$(red ✗ Failed to uninstall ${name}.)"
    exit 1
  fi

}

# :command.function
gt_uninstall_antidote_command() {
  # src/commands/uninstall/antidote.sh
  name="antidote"
  target="/usr/local/share/antidote"

  if [ ! -d "$target" ]; then
    echo "$(yellow ${name} is not installed) at ${target}."
    exit 0
  fi

  echo -n "Are you sure you want to uninstall $(cyan_bold "${name}") at $target? [y/N] "
  read -r response
  if [[ ! "$response" =~ ^[Yy]$ ]]; then
    echo "Aborted."
    exit 0
  fi

  echo "Uninstalling $(cyan_bold "${name}")..."
  remove_git_repo "$target"

  if [ ! -d "$target" ]; then
    echo "$(green_bold ✓) ${name} uninstalled successfully."
  else
    echo "$(red ✗ Failed to uninstall ${name}.)"
    exit 1
  fi

}

# :command.function
gt_uninstall_thefuck_command() {
  # src/commands/uninstall/thefuck.sh
  run_generic_uninstall "thefuck"

}

# :command.function
gt_uninstall_argocd_command() {
  # src/commands/uninstall/argocd.sh
  run_generic_uninstall "argocd"

}

# :command.function
gt_uninstall_velero_command() {
  # src/commands/uninstall/velero.sh
  run_generic_uninstall "velero"

}

# :command.function
gt_uninstall_kubeconform_command() {
  # src/commands/uninstall/kubeconform.sh
  run_generic_uninstall "kubeconform"

}

# :command.function
gt_uninstall_krew_command() {
  # src/commands/uninstall/krew.sh
  run_generic_uninstall "krew"

}

# :command.function
gt_uninstall_kubent_command() {
  # src/commands/uninstall/kubent.sh
  run_generic_uninstall "kubent"

}

# :command.function
gt_uninstall_sops_command() {
  # src/commands/uninstall/sops.sh
  run_generic_uninstall "sops"

}

# :command.function
gt_uninstall_age_command() {
  # src/commands/uninstall/age.sh
  run_generic_uninstall "age"

}

# :command.function
gt_uninstall_trivy_command() {
  # src/commands/uninstall/trivy.sh
  run_generic_uninstall "trivy"

}

# :command.function
gt_uninstall_kubens_command() {
  # src/commands/uninstall/kubens.sh
  run_generic_uninstall "kubens"

}

# :command.function
gt_uninstall_kubecolor_command() {
  # src/commands/uninstall/kubecolor.sh
  run_generic_uninstall "kubecolor"

}

# :command.function
gt_uninstall_popeye_command() {
  # src/commands/uninstall/popeye.sh
  run_generic_uninstall "popeye"

}

# :command.function
gt_uninstall_kube_linter_command() {
  # src/commands/uninstall/kube-linter.sh
  run_generic_uninstall "kube-linter"

}

# :command.function
gt_uninstall_kyverno_command() {
  # src/commands/uninstall/kyverno.sh
  run_generic_uninstall "kyverno"

}

# :command.function
gt_uninstall_conftest_command() {
  # src/commands/uninstall/conftest.sh
  run_generic_uninstall "conftest"

}

# :command.function
gt_uninstall_vault_command() {
  # src/commands/uninstall/vault.sh
  run_generic_uninstall "vault"

}

# :command.function
gt_uninstall_kubeseal_command() {
  # src/commands/uninstall/kubeseal.sh
  run_generic_uninstall "kubeseal"

}

# :command.function
gt_uninstall_zoxide_command() {
  # src/commands/uninstall/zoxide.sh
  uninstall_tool "zoxide" "/usr/local/bin/zoxide"

}

# :command.function
gt_uninstall_eza_command() {
  # src/commands/uninstall/eza.sh
  uninstall_tool "eza" "/usr/local/bin/eza"

}

# :command.function
gt_uninstall_bat_command() {
  # src/commands/uninstall/bat.sh
  uninstall_tool "bat" "/usr/local/bin/bat"

}

# :command.function
gt_uninstall_ripgrep_command() {
  # src/commands/uninstall/ripgrep.sh
  uninstall_tool "ripgrep" "/usr/local/bin/rg"

}

# :command.function
gt_uninstall_starship_command() {
  # src/commands/uninstall/starship.sh
  uninstall_tool "starship" "/usr/local/bin/starship"

}

# :command.function
gt_uninstall_fzf_command() {
  # src/commands/uninstall/fzf.sh
  uninstall_tool "fzf" "/usr/local/bin/fzf"

}

# :command.function
gt_uninstall_fd_command() {
  # src/commands/uninstall/fd.sh
  uninstall_tool "fd" "/usr/local/bin/fd"

}

# :command.function
gt_uninstall_mcfly_command() {
  # src/commands/uninstall/mcfly.sh
  uninstall_tool "mcfly" "/usr/local/bin/mcfly"

}

# :command.function
gt_uninstall_infisical_command() {
  # src/commands/uninstall/infisical.sh
  uninstall_tool "infisical" "/usr/local/bin/infisical"

}

# :command.function
gt_uninstall_ksops_command() {
  # src/commands/uninstall/ksops.sh
  uninstall_tool "ksops" "/usr/local/bin/ksops"

}

# :command.function
gt_uninstall_skopeo_command() {
  # src/commands/uninstall/skopeo.sh
  name="skopeo"

  if ! command -v skopeo >/dev/null 2>&1; then
    echo "$(yellow ${name} is not installed.)"
    exit 0
  fi

  echo -n "Are you sure you want to uninstall $(cyan_bold "${name}")? [y/N] "
  read -r response
  if [[ ! "$response" =~ ^[Yy]$ ]]; then
    echo "Aborted."
    exit 0
  fi

  remove_package "$name"
  echo "$(green_bold ✓) ${name} uninstalled."

}

# :command.function
gt_uninstall_helmfile_command() {
  # src/commands/uninstall/helmfile.sh
  uninstall_tool "helmfile" "/usr/local/bin/helmfile"

}

# :command.function
gt_uninstall_helm_command() {
  # src/commands/uninstall/helm.sh
  uninstall_tool "helm" "/usr/local/bin/helm"

}

# :command.function
gt_uninstall_chezmoi_command() {
  # src/commands/uninstall/chezmoi.sh
  uninstall_tool "chezmoi" "/usr/local/bin/chezmoi"

}

# :command.function
gt_uninstall_tmux_command() {
  # src/commands/uninstall/tmux.sh
  name="tmux"

  if ! command -v tmux >/dev/null 2>&1; then
    echo "$(yellow ${name} is not installed.)"
    exit 0
  fi

  echo -n "Are you sure you want to uninstall $(cyan_bold "${name}")? [y/N] "
  read -r response
  if [[ ! "$response" =~ ^[Yy]$ ]]; then
    echo "Aborted."
    exit 0
  fi

  remove_package "$name"
  echo "$(green_bold ✓) ${name} uninstalled."

}

# :command.function
gt_uninstall_rsync_command() {
  # src/commands/uninstall/rsync.sh
  name="rsync"

  if ! command -v rsync >/dev/null 2>&1; then
    echo "$(yellow ${name} is not installed.)"
    exit 0
  fi

  echo -n "Are you sure you want to uninstall $(cyan_bold "${name}")? [y/N] "
  read -r response
  if [[ ! "$response" =~ ^[Yy]$ ]]; then
    echo "Aborted."
    exit 0
  fi

  remove_package "$name"
  echo "$(green_bold ✓) ${name} uninstalled."

}

# :command.function
gt_uninstall_trino_command() {
  # src/commands/uninstall/trino.sh
  uninstall_tool "trino" "/usr/local/bin/trino"

}

# :command.function
gt_uninstall_nerdctl_command() {
  # src/commands/uninstall/nerdctl.sh
  uninstall_tool "nerdctl" "/usr/local/bin/nerdctl"

}

# :command.function
gt_uninstall_viddy_command() {
  # src/commands/uninstall/viddy.sh
  uninstall_tool "viddy" "/usr/local/bin/viddy"

}

# :command.function
gt_uninstall_hyperfine_command() {
  # src/commands/uninstall/hyperfine.sh
  uninstall_tool "hyperfine" "/usr/local/bin/hyperfine"

}

# :command.function
gt_uninstall_jq_command() {
  # src/commands/uninstall/jq.sh
  uninstall_tool "jq" "/usr/local/bin/jq"

}

# :command.function
gt_uninstall_yq_command() {
  # src/commands/uninstall/yq.sh
  uninstall_tool "yq" "/usr/local/bin/yq"

}

# :command.function
gt_uninstall_gum_command() {
  # src/commands/uninstall/gum.sh
  uninstall_tool "gum" "/usr/local/bin/gum"

}

# :command.function
gt_uninstall_uv_command() {
  # src/commands/uninstall/uv.sh
  uninstall_tool "uv" "/usr/local/bin/uv"

}

# :command.function
gt_uninstall_bashly_command() {
  # src/commands/uninstall/bashly.sh
  name="bashly"

  if ! command -v bashly >/dev/null 2>&1; then
    echo "$(yellow ${name} is not installed.)"
    exit 0
  fi

  echo -n "Are you sure you want to uninstall $(cyan_bold "${name}")? [y/N] "
  read -r response
  if [[ ! "$response" =~ ^[Yy]$ ]]; then
    echo "Aborted."
    exit 0
  fi

  echo "Uninstalling $(cyan_bold "${name}") via gem..."
  if sudo gem uninstall bashly; then
    echo "$(green_bold ✓) ${name} uninstalled."
  else
    echo "$(red ✗ Failed to uninstall ${name}.)"
    exit 1
  fi

}

# :command.function
gt_uninstall_gem_command() {
  # src/commands/uninstall/gem.sh
  name="gem"

  if ! command -v gem >/dev/null 2>&1; then
    echo "$(yellow ${name} is not installed.)"
    exit 0
  fi

  echo -n "Are you sure you want to uninstall $(cyan_bold "${name}")? [y/N] "
  read -r response
  if [[ ! "$response" =~ ^[Yy]$ ]]; then
    echo "Aborted."
    exit 0
  fi

  if command -v apt-get >/dev/null 2>&1; then
    pkg="ruby-dev ruby rubygems"
  elif command -v dnf >/dev/null 2>&1 || command -v yum >/dev/null 2>&1; then
    pkg="ruby-devel ruby rubygems"
  elif command -v pacman >/dev/null 2>&1; then
    pkg="ruby"
  elif command -v apk >/dev/null 2>&1; then
    pkg="ruby-dev ruby"
  else
    echo "$(red ✗) Could not detect a supported package manager."
    exit 1
  fi

  remove_package "$pkg"
  echo "$(green_bold ✓) ${name} uninstalled."

}

# :command.function
gt_uninstall_claude_command() {
  # src/commands/uninstall/claude.sh
  name="claude"

  if ! command -v claude >/dev/null 2>&1; then
    echo "$(yellow ${name} is not installed.)"
    exit 0
  fi

  echo -n "Are you sure you want to uninstall $(cyan_bold "${name}")? [y/N] "
  read -r response
  if [[ ! "$response" =~ ^[Yy]$ ]]; then
    echo "Aborted."
    exit 0
  fi

  echo "Uninstalling $(cyan_bold "${name}") via npm..."

  if npm uninstall -g @anthropic-ai/claude-code; then
    echo "$(green_bold ✓) ${name} uninstalled."
  else
    echo "$(red ✗ Failed to uninstall ${name}.)"
    exit 1
  fi

}

# :command.function
gt_uninstall_talisman_command() {
  # src/commands/uninstall/talisman.sh
  uninstall_tool "talisman" "/usr/local/bin/talisman"

}

# :command.function
gt_setup_multi_command() {
  # src/commands/setup/multi.sh
  skip_shell=${args[--skip-shell]}
  eval "raw_tools=(${args['tools']})"

  # Process both space-separated and comma-separated inputs
  tools=()
  for t in "${raw_tools[@]}"; do
    IFS=',' read -ra split_tools <<< "$t"
    for st in "${split_tools[@]}"; do
      if [[ -n "$st" ]]; then
        tools+=("$st")
      fi
    done
  done

  for tool in "${tools[@]}"; do
    # We call the internal bashly generated command function for that tool if it exists
    if declare -F "gt_setup_${tool}_command" > /dev/null; then
      echo "==========================================="
      echo "Configuring: $(cyan_bold "$tool")"
      echo "==========================================="

      # We clear existing args first
      local orig_args
      orig_args=$(declare -p args)

      # Prepare args for the specific tool command
      if [[ -n "$skip_shell" ]]; then
        args['--skip-shell']=1
      fi

      # Call the internal function in a subshell to prevent 'exit' from killing the loop
      ( gt_setup_${tool}_command ) || true

      # Restore args
      eval "$orig_args"
      echo ""
    else
      echo "$(red "Error:") Tool '$(cyan_bold "$tool")' does not have a setup command or is not found."
      echo ""
    fi
  done

}

# :command.function
gt_setup_antidote_command() {
  # src/commands/setup/antidote.sh
  skip_plugins=${args[--skip-plugins]}
  skip_zshrc=${args[--skip-zshrc]}

  name="antidote"
  zsh_plugins_file="${ZDOTDIR:-$HOME}/.zsh_plugins.txt"
  zshrc="${ZDOTDIR:-$HOME}/.zshrc"

  echo "Configuring $(cyan_bold "$name")..."
  echo ""

  # ===================================================================
  # Step 1: Check antidote installation
  # ===================================================================
  echo "$(bold "▸ Step 1/3:") Checking antidote installation..."

  antidote_path="/usr/local/share/antidote/antidote.zsh"
  if [ ! -f "$antidote_path" ] && [ -f "${ZDOTDIR:-$HOME}/.antidote/antidote.zsh" ]; then
    antidote_path="${ZDOTDIR:-$HOME}/.antidote/antidote.zsh"
  fi

  require_installed "antidote" "$antidote_path"
  echo "$(green_bold "✓") antidote found at $antidote_path"

  # ===================================================================
  # Step 2: Configure ~/.zshrc
  # ===================================================================
  echo ""
  echo "$(bold "▸ Step 2/3:") Configuring $(cyan "$zshrc")..."

  ANTIDOTE_MARKER="# --- antidote bootstrap (managed by os-scripts) ---"

  if [ -n "$skip_zshrc" ]; then
    echo "$(yellow "⚠") Skipping .zshrc configuration (--skip-zshrc)"
  else
    inject_config_block "$zshrc" "$ANTIDOTE_MARKER" "$(template_antidote_zshrc)"
  fi

  # ===================================================================
  # Step 3: Seed ~/.zsh_plugins.txt
  # ===================================================================
  echo ""
  echo "$(bold "▸ Step 3/3:") Seeding $(cyan "$zsh_plugins_file")..."

  if [ -n "$skip_plugins" ]; then
    echo "$(yellow "⚠") Skipping plugin seeding (--skip-plugins)"
  else
    seed_file "$zsh_plugins_file" "$(template_antidote_plugins)"
  fi

  # ===================================================================
  # Summary
  # ===================================================================
  echo ""
  echo "$(green_bold "━━━ Configuration complete ━━━")"
  echo ""
  echo "Next steps:"
  echo "  1. Start a new zsh session:  $(bold "exec zsh")"
  echo "  2. Edit plugins:             $(bold "vim $zsh_plugins_file")"
  echo "  3. Update plugins:           $(bold "antidote update")"
  echo "  4. Docs:                     $(bold "docs/antidote-deployment.md")"

}

# :command.function
gt_setup_starship_command() {
  # src/commands/setup/starship.sh
  preset=${args[--preset]}
  skip_config=${args[--skip-config]}
  force_config=${args[--force-config]}

  name="starship"
  config_file="${STARSHIP_CONFIG:-${XDG_CONFIG_HOME:-$HOME/.config}/starship.toml}"

  echo "Configuring $(cyan_bold "$name")..."
  echo ""

  # ===================================================================
  # Step 1: Check starship installation
  # ===================================================================
  echo "$(bold "▸ Step 1/2:") Checking starship installation..."

  starship_bin=$(command -v starship 2>/dev/null || true)
  require_installed "starship" "$starship_bin"
  starship_version=$(starship --version 2>/dev/null | awk 'NR==1 {print $2}')
  echo "$(green_bold "✓") starship found at $starship_bin (v${starship_version:-unknown})"

  # ===================================================================
  # Step 2: Seed ~/.config/starship.toml
  # ===================================================================
  echo ""
  echo "$(bold "▸ Step 2/2:") Seeding $(cyan "$config_file")..."

  if [ -n "$skip_config" ]; then
    echo "$(yellow "⚠") Skipping config (--skip-config)"
  else
    mkdir -p "$(dirname "$config_file")"

    if [ -n "$force_config" ] && [ -f "$config_file" ]; then
      rm -f "$config_file"
    fi

    if [ -n "$preset" ]; then
      if [ -s "$config_file" ]; then
        echo "$(yellow "⚠") $config_file already has content — use $(bold --force-config) to overwrite"
      elif starship preset "$preset" -o "$config_file" 2>/dev/null; then
        echo "$(green_bold "✓") Applied preset $(bold "$preset") to $config_file"
      else
        echo "$(red "✗") Unknown preset: $(bold "$preset")"
        echo "  Run $(bold "starship preset list") to see available presets."
        exit 1
      fi
    else
      seed_file "$config_file" "$(template_starship_config)"
    fi
  fi

  # ===================================================================
  # Summary
  # ===================================================================
  echo ""
  echo "$(green_bold "━━━ Configuration complete ━━━")"
  echo ""
  echo "Next steps:"
  echo "  1. Reload shell:        $(bold "exec \$SHELL")"
  echo "  2. Edit config:         $(bold "vim $config_file")"
  echo "  3. Browse presets:      $(bold "starship preset list")"
  echo "  4. Apply preset later:  $(bold "starship preset tokyo-night -o $config_file")"
  echo "  5. Docs:                $(bold "https://starship.rs/config/")"

}

# :command.function
gt_outdated_command() {
  # src/commands/outdated.sh
  all=${args[--all]}
  target_tool=${args[tool]}

  # Dynamically get the list of available tools from the help menu
  available_tools=()
  while read -r tool; do
    if [[ -n "$tool" && "$tool" != "multi" ]]; then
      available_tools+=("$tool")
    fi
  done < <("$0" install --help | awk '/^  [a-z0-9A-Z]/{print $1}')

  if [ -n "$target_tool" ]; then
    tools=("$target_tool")
    all=1 # Force check even if not installed
  else
    tools=("${available_tools[@]}")
  fi

  echo "Checking for updates..."
  echo ""

  for tool in "${tools[@]}"; do
    local_ver=$(get_local_version "$tool")

    if [ -z "$local_ver" ]; then
      if [ -n "$all" ]; then
        printf "  %-12s %s\n" "$tool" "[Not installed]"
      fi
      continue
    fi

    remote_ver=$(get_remote_version "$tool")

    if [ -z "$remote_ver" ]; then
      printf "  %-12s %s\n" "$tool" "$(yellow "Error fetching remote version")"
      continue
    fi

    if [ "$local_ver" == "$remote_ver" ]; then
      printf "$(green_bold "[Up-to-date]") %-12s (v%s)\n" "$tool" "$local_ver"
    else
      printf "$(red_bold "[Outdated]")   %-12s v%s -> v%s\n" "$tool" "$local_ver" "$remote_ver"
    fi
  done
  echo ""

}

# :command.function
gt_completions_command() {
  # src/commands/completions.sh
  send_completions

}

# :command.parse_requirements
parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --version | -v)
        version_command
        exit
        ;;

      --help | -h)
        long_usage=yes
        gt_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.environment_variables_filter

  env_var_names+=("GITHUB_TOKEN")
  env_var_names+=("GT_DOWNLOAD_TIMEOUT")

  # :command.dependencies_filter
  if command -v curl >/dev/null 2>&1; then
    deps['curl']="$(command -v curl | head -n1)"
  else
    printf "missing dependency: curl\n" >&2
    exit 1
  fi

  if command -v tar >/dev/null 2>&1; then
    deps['tar']="$(command -v tar | head -n1)"
  else
    printf "missing dependency: tar\n" >&2
    exit 1
  fi

  # :command.command_filter
  action=${1:-}

  case $action in
    -*) ;;

    install)
      action="install"
      shift
      gt_install_parse_requirements "$@"
      shift $#
      ;;

    uninstall)
      action="uninstall"
      shift
      gt_uninstall_parse_requirements "$@"
      shift $#
      ;;

    setup)
      action="setup"
      shift
      gt_setup_parse_requirements "$@"
      shift $#
      ;;

    outdated)
      action="outdated"
      shift
      gt_outdated_parse_requirements "$@"
      shift $#
      ;;

    completions)
      action="completions"
      shift
      gt_completions_parse_requirements "$@"
      shift $#
      ;;

    # :command.command_fallback
    "")
      gt_usage >&2
      exit 1
      ;;

    *)
      printf "invalid command: %s\n" "$action" >&2
      exit 1
      ;;

  esac

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_install_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action=${1:-}

  case $action in
    -*) ;;

    multi)
      action="multi"
      shift
      gt_install_multi_parse_requirements "$@"
      shift $#
      ;;

    kustomize)
      action="kustomize"
      shift
      gt_install_kustomize_parse_requirements "$@"
      shift $#
      ;;

    k9s)
      action="k9s"
      shift
      gt_install_k9s_parse_requirements "$@"
      shift $#
      ;;

    kubectl)
      action="kubectl"
      shift
      gt_install_kubectl_parse_requirements "$@"
      shift $#
      ;;

    fastfetch)
      action="fastfetch"
      shift
      gt_install_fastfetch_parse_requirements "$@"
      shift $#
      ;;

    direnv)
      action="direnv"
      shift
      gt_install_direnv_parse_requirements "$@"
      shift $#
      ;;

    btop)
      action="btop"
      shift
      gt_install_btop_parse_requirements "$@"
      shift $#
      ;;

    zsh)
      action="zsh"
      shift
      gt_install_zsh_parse_requirements "$@"
      shift $#
      ;;

    antidote)
      action="antidote"
      shift
      gt_install_antidote_parse_requirements "$@"
      shift $#
      ;;

    thefuck)
      action="thefuck"
      shift
      gt_install_thefuck_parse_requirements "$@"
      shift $#
      ;;

    argocd)
      action="argocd"
      shift
      gt_install_argocd_parse_requirements "$@"
      shift $#
      ;;

    velero)
      action="velero"
      shift
      gt_install_velero_parse_requirements "$@"
      shift $#
      ;;

    kubeconform)
      action="kubeconform"
      shift
      gt_install_kubeconform_parse_requirements "$@"
      shift $#
      ;;

    krew)
      action="krew"
      shift
      gt_install_krew_parse_requirements "$@"
      shift $#
      ;;

    kubent)
      action="kubent"
      shift
      gt_install_kubent_parse_requirements "$@"
      shift $#
      ;;

    sops)
      action="sops"
      shift
      gt_install_sops_parse_requirements "$@"
      shift $#
      ;;

    age)
      action="age"
      shift
      gt_install_age_parse_requirements "$@"
      shift $#
      ;;

    trivy)
      action="trivy"
      shift
      gt_install_trivy_parse_requirements "$@"
      shift $#
      ;;

    kubens)
      action="kubens"
      shift
      gt_install_kubens_parse_requirements "$@"
      shift $#
      ;;

    kubecolor)
      action="kubecolor"
      shift
      gt_install_kubecolor_parse_requirements "$@"
      shift $#
      ;;

    popeye)
      action="popeye"
      shift
      gt_install_popeye_parse_requirements "$@"
      shift $#
      ;;

    kube-linter)
      action="kube-linter"
      shift
      gt_install_kube_linter_parse_requirements "$@"
      shift $#
      ;;

    kyverno)
      action="kyverno"
      shift
      gt_install_kyverno_parse_requirements "$@"
      shift $#
      ;;

    conftest)
      action="conftest"
      shift
      gt_install_conftest_parse_requirements "$@"
      shift $#
      ;;

    vault)
      action="vault"
      shift
      gt_install_vault_parse_requirements "$@"
      shift $#
      ;;

    kubeseal)
      action="kubeseal"
      shift
      gt_install_kubeseal_parse_requirements "$@"
      shift $#
      ;;

    zoxide)
      action="zoxide"
      shift
      gt_install_zoxide_parse_requirements "$@"
      shift $#
      ;;

    eza)
      action="eza"
      shift
      gt_install_eza_parse_requirements "$@"
      shift $#
      ;;

    bat)
      action="bat"
      shift
      gt_install_bat_parse_requirements "$@"
      shift $#
      ;;

    ripgrep)
      action="ripgrep"
      shift
      gt_install_ripgrep_parse_requirements "$@"
      shift $#
      ;;

    starship)
      action="starship"
      shift
      gt_install_starship_parse_requirements "$@"
      shift $#
      ;;

    fzf)
      action="fzf"
      shift
      gt_install_fzf_parse_requirements "$@"
      shift $#
      ;;

    fd)
      action="fd"
      shift
      gt_install_fd_parse_requirements "$@"
      shift $#
      ;;

    mcfly)
      action="mcfly"
      shift
      gt_install_mcfly_parse_requirements "$@"
      shift $#
      ;;

    infisical)
      action="infisical"
      shift
      gt_install_infisical_parse_requirements "$@"
      shift $#
      ;;

    ksops)
      action="ksops"
      shift
      gt_install_ksops_parse_requirements "$@"
      shift $#
      ;;

    skopeo)
      action="skopeo"
      shift
      gt_install_skopeo_parse_requirements "$@"
      shift $#
      ;;

    helmfile)
      action="helmfile"
      shift
      gt_install_helmfile_parse_requirements "$@"
      shift $#
      ;;

    helm)
      action="helm"
      shift
      gt_install_helm_parse_requirements "$@"
      shift $#
      ;;

    chezmoi)
      action="chezmoi"
      shift
      gt_install_chezmoi_parse_requirements "$@"
      shift $#
      ;;

    tmux)
      action="tmux"
      shift
      gt_install_tmux_parse_requirements "$@"
      shift $#
      ;;

    rsync)
      action="rsync"
      shift
      gt_install_rsync_parse_requirements "$@"
      shift $#
      ;;

    trino)
      action="trino"
      shift
      gt_install_trino_parse_requirements "$@"
      shift $#
      ;;

    nerdctl)
      action="nerdctl"
      shift
      gt_install_nerdctl_parse_requirements "$@"
      shift $#
      ;;

    viddy)
      action="viddy"
      shift
      gt_install_viddy_parse_requirements "$@"
      shift $#
      ;;

    hyperfine)
      action="hyperfine"
      shift
      gt_install_hyperfine_parse_requirements "$@"
      shift $#
      ;;

    jq)
      action="jq"
      shift
      gt_install_jq_parse_requirements "$@"
      shift $#
      ;;

    yq)
      action="yq"
      shift
      gt_install_yq_parse_requirements "$@"
      shift $#
      ;;

    gum)
      action="gum"
      shift
      gt_install_gum_parse_requirements "$@"
      shift $#
      ;;

    uv)
      action="uv"
      shift
      gt_install_uv_parse_requirements "$@"
      shift $#
      ;;

    bashly)
      action="bashly"
      shift
      gt_install_bashly_parse_requirements "$@"
      shift $#
      ;;

    gem)
      action="gem"
      shift
      gt_install_gem_parse_requirements "$@"
      shift $#
      ;;

    claude)
      action="claude"
      shift
      gt_install_claude_parse_requirements "$@"
      shift $#
      ;;

    talisman)
      action="talisman"
      shift
      gt_install_talisman_parse_requirements "$@"
      shift $#
      ;;

    # :command.command_fallback
    "")
      gt_install_usage >&2
      exit 1
      ;;

    *)
      printf "invalid command: %s\n" "$action" >&2
      exit 1
      ;;

  esac

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_install_multi_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_multi_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install multi"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_repeatable
        # :argument.case_repeatable
        escaped="$(printf '%q' "$1")"
        if [[ -z ${args['tools']+x} ]]; then
          args['tools']="$escaped"
        else
          args['tools']="${args['tools']} $escaped"

        fi
        shift

        ;;

    esac
  done
  # :command.required_args_filter
  if [[ -z ${args['tools']+x} ]]; then
    printf "missing required argument: TOOLS\nusage: gt install multi TOOLS... [OPTIONS]\n" >&2
    exit 1
  fi

}

# :command.parse_requirements
gt_install_kustomize_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_kustomize_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install kustomize"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_k9s_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_k9s_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install k9s"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_kubectl_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_kubectl_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install kubectl"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_fastfetch_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_fastfetch_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install fastfetch"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_direnv_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_direnv_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install direnv"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_btop_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_btop_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install btop"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_zsh_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_zsh_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install zsh"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_antidote_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_antidote_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install antidote"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_thefuck_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_thefuck_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install thefuck"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_argocd_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_argocd_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install argocd"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_velero_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_velero_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install velero"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_kubeconform_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_kubeconform_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install kubeconform"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_krew_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_krew_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install krew"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_kubent_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_kubent_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install kubent"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_sops_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_sops_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install sops"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_age_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_age_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install age"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_trivy_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_trivy_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install trivy"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_kubens_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_kubens_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install kubens"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_kubecolor_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_kubecolor_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install kubecolor"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_popeye_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_popeye_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install popeye"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_kube_linter_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_kube_linter_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install kube-linter"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_kyverno_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_kyverno_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install kyverno"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_conftest_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_conftest_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install conftest"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_vault_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_vault_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.dependencies_filter
  if command -v unzip >/dev/null 2>&1; then
    deps['unzip']="$(command -v unzip | head -n1)"
  else
    printf "missing dependency: unzip\n" >&2
    exit 1
  fi

  # :command.command_filter
  action="install vault"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_kubeseal_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_kubeseal_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install kubeseal"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_zoxide_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_zoxide_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install zoxide"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_eza_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_eza_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install eza"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_bat_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_bat_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install bat"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_ripgrep_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_ripgrep_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install ripgrep"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_starship_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_starship_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install starship"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_fzf_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_fzf_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install fzf"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_fd_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_fd_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install fd"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_mcfly_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_mcfly_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install mcfly"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_infisical_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_infisical_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install infisical"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_ksops_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_ksops_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install ksops"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_skopeo_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_skopeo_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install skopeo"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_install_helmfile_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_helmfile_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install helmfile"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_helm_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_helm_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install helm"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_chezmoi_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_chezmoi_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install chezmoi"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_tmux_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_tmux_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install tmux"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_install_rsync_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_rsync_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install rsync"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_install_trino_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_trino_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install trino"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_nerdctl_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_nerdctl_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install nerdctl"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_viddy_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_viddy_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install viddy"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_hyperfine_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_hyperfine_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install hyperfine"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_jq_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_jq_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install jq"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_yq_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_yq_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install yq"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_gum_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_gum_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install gum"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_uv_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_uv_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install uv"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_bashly_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_bashly_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.dependencies_filter
  if command -v gem >/dev/null 2>&1; then
    deps['gem']="$(command -v gem | head -n1)"
  else
    printf "missing dependency: gem\n" >&2
    exit 1
  fi

  # :command.command_filter
  action="install bashly"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_gem_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_gem_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install gem"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_install_claude_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_claude_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.dependencies_filter
  if command -v npm >/dev/null 2>&1; then
    deps['npm']="$(command -v npm | head -n1)"
  else
    printf "missing dependency: npm\n" >&2
    exit 1
  fi

  # :command.command_filter
  action="install claude"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_install_talisman_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_install_talisman_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="install talisman"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --version | -v)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--version']="$2"
          shift
          shift
        else
          printf "%s\n" "--version requires an argument: --version, -v VERSION" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --force | -f)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['--version']:-} ]] || args['--version']="latest"

}

# :command.parse_requirements
gt_uninstall_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action=${1:-}

  case $action in
    -*) ;;

    kustomize)
      action="kustomize"
      shift
      gt_uninstall_kustomize_parse_requirements "$@"
      shift $#
      ;;

    k9s)
      action="k9s"
      shift
      gt_uninstall_k9s_parse_requirements "$@"
      shift $#
      ;;

    kubectl)
      action="kubectl"
      shift
      gt_uninstall_kubectl_parse_requirements "$@"
      shift $#
      ;;

    fastfetch)
      action="fastfetch"
      shift
      gt_uninstall_fastfetch_parse_requirements "$@"
      shift $#
      ;;

    direnv)
      action="direnv"
      shift
      gt_uninstall_direnv_parse_requirements "$@"
      shift $#
      ;;

    btop)
      action="btop"
      shift
      gt_uninstall_btop_parse_requirements "$@"
      shift $#
      ;;

    zsh)
      action="zsh"
      shift
      gt_uninstall_zsh_parse_requirements "$@"
      shift $#
      ;;

    antidote)
      action="antidote"
      shift
      gt_uninstall_antidote_parse_requirements "$@"
      shift $#
      ;;

    thefuck)
      action="thefuck"
      shift
      gt_uninstall_thefuck_parse_requirements "$@"
      shift $#
      ;;

    argocd)
      action="argocd"
      shift
      gt_uninstall_argocd_parse_requirements "$@"
      shift $#
      ;;

    velero)
      action="velero"
      shift
      gt_uninstall_velero_parse_requirements "$@"
      shift $#
      ;;

    kubeconform)
      action="kubeconform"
      shift
      gt_uninstall_kubeconform_parse_requirements "$@"
      shift $#
      ;;

    krew)
      action="krew"
      shift
      gt_uninstall_krew_parse_requirements "$@"
      shift $#
      ;;

    kubent)
      action="kubent"
      shift
      gt_uninstall_kubent_parse_requirements "$@"
      shift $#
      ;;

    sops)
      action="sops"
      shift
      gt_uninstall_sops_parse_requirements "$@"
      shift $#
      ;;

    age)
      action="age"
      shift
      gt_uninstall_age_parse_requirements "$@"
      shift $#
      ;;

    trivy)
      action="trivy"
      shift
      gt_uninstall_trivy_parse_requirements "$@"
      shift $#
      ;;

    kubens)
      action="kubens"
      shift
      gt_uninstall_kubens_parse_requirements "$@"
      shift $#
      ;;

    kubecolor)
      action="kubecolor"
      shift
      gt_uninstall_kubecolor_parse_requirements "$@"
      shift $#
      ;;

    popeye)
      action="popeye"
      shift
      gt_uninstall_popeye_parse_requirements "$@"
      shift $#
      ;;

    kube-linter)
      action="kube-linter"
      shift
      gt_uninstall_kube_linter_parse_requirements "$@"
      shift $#
      ;;

    kyverno)
      action="kyverno"
      shift
      gt_uninstall_kyverno_parse_requirements "$@"
      shift $#
      ;;

    conftest)
      action="conftest"
      shift
      gt_uninstall_conftest_parse_requirements "$@"
      shift $#
      ;;

    vault)
      action="vault"
      shift
      gt_uninstall_vault_parse_requirements "$@"
      shift $#
      ;;

    kubeseal)
      action="kubeseal"
      shift
      gt_uninstall_kubeseal_parse_requirements "$@"
      shift $#
      ;;

    zoxide)
      action="zoxide"
      shift
      gt_uninstall_zoxide_parse_requirements "$@"
      shift $#
      ;;

    eza)
      action="eza"
      shift
      gt_uninstall_eza_parse_requirements "$@"
      shift $#
      ;;

    bat)
      action="bat"
      shift
      gt_uninstall_bat_parse_requirements "$@"
      shift $#
      ;;

    ripgrep)
      action="ripgrep"
      shift
      gt_uninstall_ripgrep_parse_requirements "$@"
      shift $#
      ;;

    starship)
      action="starship"
      shift
      gt_uninstall_starship_parse_requirements "$@"
      shift $#
      ;;

    fzf)
      action="fzf"
      shift
      gt_uninstall_fzf_parse_requirements "$@"
      shift $#
      ;;

    fd)
      action="fd"
      shift
      gt_uninstall_fd_parse_requirements "$@"
      shift $#
      ;;

    mcfly)
      action="mcfly"
      shift
      gt_uninstall_mcfly_parse_requirements "$@"
      shift $#
      ;;

    infisical)
      action="infisical"
      shift
      gt_uninstall_infisical_parse_requirements "$@"
      shift $#
      ;;

    ksops)
      action="ksops"
      shift
      gt_uninstall_ksops_parse_requirements "$@"
      shift $#
      ;;

    skopeo)
      action="skopeo"
      shift
      gt_uninstall_skopeo_parse_requirements "$@"
      shift $#
      ;;

    helmfile)
      action="helmfile"
      shift
      gt_uninstall_helmfile_parse_requirements "$@"
      shift $#
      ;;

    helm)
      action="helm"
      shift
      gt_uninstall_helm_parse_requirements "$@"
      shift $#
      ;;

    chezmoi)
      action="chezmoi"
      shift
      gt_uninstall_chezmoi_parse_requirements "$@"
      shift $#
      ;;

    tmux)
      action="tmux"
      shift
      gt_uninstall_tmux_parse_requirements "$@"
      shift $#
      ;;

    rsync)
      action="rsync"
      shift
      gt_uninstall_rsync_parse_requirements "$@"
      shift $#
      ;;

    trino)
      action="trino"
      shift
      gt_uninstall_trino_parse_requirements "$@"
      shift $#
      ;;

    nerdctl)
      action="nerdctl"
      shift
      gt_uninstall_nerdctl_parse_requirements "$@"
      shift $#
      ;;

    viddy)
      action="viddy"
      shift
      gt_uninstall_viddy_parse_requirements "$@"
      shift $#
      ;;

    hyperfine)
      action="hyperfine"
      shift
      gt_uninstall_hyperfine_parse_requirements "$@"
      shift $#
      ;;

    jq)
      action="jq"
      shift
      gt_uninstall_jq_parse_requirements "$@"
      shift $#
      ;;

    yq)
      action="yq"
      shift
      gt_uninstall_yq_parse_requirements "$@"
      shift $#
      ;;

    gum)
      action="gum"
      shift
      gt_uninstall_gum_parse_requirements "$@"
      shift $#
      ;;

    uv)
      action="uv"
      shift
      gt_uninstall_uv_parse_requirements "$@"
      shift $#
      ;;

    bashly)
      action="bashly"
      shift
      gt_uninstall_bashly_parse_requirements "$@"
      shift $#
      ;;

    gem)
      action="gem"
      shift
      gt_uninstall_gem_parse_requirements "$@"
      shift $#
      ;;

    claude)
      action="claude"
      shift
      gt_uninstall_claude_parse_requirements "$@"
      shift $#
      ;;

    talisman)
      action="talisman"
      shift
      gt_uninstall_talisman_parse_requirements "$@"
      shift $#
      ;;

    # :command.command_fallback
    "")
      gt_uninstall_usage >&2
      exit 1
      ;;

    *)
      printf "invalid command: %s\n" "$action" >&2
      exit 1
      ;;

  esac

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_kustomize_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_kustomize_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall kustomize"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_k9s_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_k9s_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall k9s"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_kubectl_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_kubectl_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall kubectl"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_fastfetch_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_fastfetch_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall fastfetch"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_direnv_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_direnv_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall direnv"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_btop_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_btop_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall btop"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_zsh_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_zsh_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall zsh"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_antidote_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_antidote_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall antidote"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_thefuck_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_thefuck_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall thefuck"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_argocd_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_argocd_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall argocd"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_velero_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_velero_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall velero"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_kubeconform_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_kubeconform_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall kubeconform"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_krew_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_krew_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall krew"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_kubent_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_kubent_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall kubent"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_sops_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_sops_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall sops"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_age_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_age_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall age"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_trivy_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_trivy_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall trivy"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_kubens_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_kubens_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall kubens"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_kubecolor_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_kubecolor_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall kubecolor"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_popeye_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_popeye_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall popeye"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_kube_linter_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_kube_linter_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall kube-linter"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_kyverno_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_kyverno_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall kyverno"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_conftest_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_conftest_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall conftest"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_vault_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_vault_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall vault"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_kubeseal_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_kubeseal_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall kubeseal"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_zoxide_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_zoxide_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall zoxide"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_eza_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_eza_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall eza"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_bat_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_bat_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall bat"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_ripgrep_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_ripgrep_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall ripgrep"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_starship_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_starship_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall starship"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_fzf_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_fzf_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall fzf"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_fd_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_fd_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall fd"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_mcfly_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_mcfly_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall mcfly"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_infisical_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_infisical_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall infisical"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_ksops_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_ksops_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall ksops"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_skopeo_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_skopeo_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall skopeo"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_helmfile_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_helmfile_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall helmfile"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_helm_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_helm_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall helm"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_chezmoi_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_chezmoi_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall chezmoi"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_tmux_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_tmux_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall tmux"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_rsync_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_rsync_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall rsync"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_trino_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_trino_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall trino"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_nerdctl_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_nerdctl_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall nerdctl"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_viddy_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_viddy_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall viddy"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_hyperfine_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_hyperfine_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall hyperfine"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_jq_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_jq_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall jq"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_yq_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_yq_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall yq"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_gum_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_gum_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall gum"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_uv_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_uv_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall uv"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_bashly_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_bashly_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.dependencies_filter
  if command -v gem >/dev/null 2>&1; then
    deps['gem']="$(command -v gem | head -n1)"
  else
    printf "missing dependency: gem\n" >&2
    exit 1
  fi

  # :command.command_filter
  action="uninstall bashly"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_gem_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_gem_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall gem"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_claude_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_claude_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.dependencies_filter
  if command -v npm >/dev/null 2>&1; then
    deps['npm']="$(command -v npm | head -n1)"
  else
    printf "missing dependency: npm\n" >&2
    exit 1
  fi

  # :command.command_filter
  action="uninstall claude"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_uninstall_talisman_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_uninstall_talisman_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="uninstall talisman"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_setup_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_setup_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action=${1:-}

  case $action in
    -*) ;;

    multi)
      action="multi"
      shift
      gt_setup_multi_parse_requirements "$@"
      shift $#
      ;;

    antidote)
      action="antidote"
      shift
      gt_setup_antidote_parse_requirements "$@"
      shift $#
      ;;

    starship)
      action="starship"
      shift
      gt_setup_starship_parse_requirements "$@"
      shift $#
      ;;

    # :command.command_fallback
    "")
      gt_setup_usage >&2
      exit 1
      ;;

    *)
      printf "invalid command: %s\n" "$action" >&2
      exit 1
      ;;

  esac

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_setup_multi_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_setup_multi_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="setup multi"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --skip-shell)

        # :flag.case_no_arg
        args['--skip-shell']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_repeatable
        # :argument.case_repeatable
        escaped="$(printf '%q' "$1")"
        if [[ -z ${args['tools']+x} ]]; then
          args['tools']="$escaped"
        else
          args['tools']="${args['tools']} $escaped"

        fi
        shift

        ;;

    esac
  done
  # :command.required_args_filter
  if [[ -z ${args['tools']+x} ]]; then
    printf "missing required argument: TOOLS\nusage: gt setup multi TOOLS... [OPTIONS]\n" >&2
    exit 1
  fi

}

# :command.parse_requirements
gt_setup_antidote_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_setup_antidote_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.dependencies_filter
  if command -v zsh >/dev/null 2>&1; then
    deps['zsh']="$(command -v zsh | head -n1)"
  else
    printf "missing dependency: zsh\n" >&2
    exit 1
  fi

  if command -v git >/dev/null 2>&1; then
    deps['git']="$(command -v git | head -n1)"
  else
    printf "missing dependency: git\n" >&2
    exit 1
  fi

  # :command.command_filter
  action="setup antidote"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --skip-plugins)

        # :flag.case_no_arg
        args['--skip-plugins']=1
        shift
        ;;

      # :flag.case
      --skip-zshrc)

        # :flag.case_no_arg
        args['--skip-zshrc']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_setup_starship_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_setup_starship_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="setup starship"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --preset)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--preset']="$2"
          shift
          shift
        else
          printf "%s\n" "--preset requires an argument: --preset NAME" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --skip-config)

        # :flag.case_no_arg
        args['--skip-config']=1
        shift
        ;;

      # :flag.case
      --force-config)

        # :flag.case_no_arg
        args['--force-config']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
gt_outdated_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_outdated_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="outdated"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --all | -a)

        # :flag.case_no_arg
        args['--all']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        # :argument.case
        if [[ -z ${args['tool']+x} ]]; then
          args['tool']=$1
          shift
        else
          printf "invalid argument: %s\n" "$key" >&2
          exit 1
        fi

        ;;

    esac
  done

}

# :command.parse_requirements
gt_completions_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        gt_completions_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="completions"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        # :argument.case
        if [[ -z ${args['shell']+x} ]]; then
          args['shell']=$1
          shift
        else
          printf "invalid argument: %s\n" "$key" >&2
          exit 1
        fi

        ;;

    esac
  done

  # :command.default_assignments
  [[ -n ${args['shell']:-} ]] || args['shell']="bash"

  # :command.whitelist_filter
  if [[ -n ${args['shell']:-} ]] && [[ ! ${args['shell']:-} =~ ^(bash|zsh)$ ]]; then
    printf "%s\n" "shell must be one of: bash, zsh" >&2
    exit 1
  fi

}

# :command.initialize
initialize() {
  version="0.1.0"
  long_usage=''
  set -e

}

# :command.run
run() {
  declare -A args=()
  declare -A deps=()
  declare -a other_args=()
  declare -a env_var_names=()
  declare -a input=()
  normalize_input "$@"
  parse_requirements "${input[@]}"

  case "$action" in
    "install") gt_install_command ;;
    "install multi") gt_install_multi_command ;;
    "install kustomize") gt_install_kustomize_command ;;
    "install k9s") gt_install_k9s_command ;;
    "install kubectl") gt_install_kubectl_command ;;
    "install fastfetch") gt_install_fastfetch_command ;;
    "install direnv") gt_install_direnv_command ;;
    "install btop") gt_install_btop_command ;;
    "install zsh") gt_install_zsh_command ;;
    "install antidote") gt_install_antidote_command ;;
    "install thefuck") gt_install_thefuck_command ;;
    "install argocd") gt_install_argocd_command ;;
    "install velero") gt_install_velero_command ;;
    "install kubeconform") gt_install_kubeconform_command ;;
    "install krew") gt_install_krew_command ;;
    "install kubent") gt_install_kubent_command ;;
    "install sops") gt_install_sops_command ;;
    "install age") gt_install_age_command ;;
    "install trivy") gt_install_trivy_command ;;
    "install kubens") gt_install_kubens_command ;;
    "install kubecolor") gt_install_kubecolor_command ;;
    "install popeye") gt_install_popeye_command ;;
    "install kube-linter") gt_install_kube_linter_command ;;
    "install kyverno") gt_install_kyverno_command ;;
    "install conftest") gt_install_conftest_command ;;
    "install vault") gt_install_vault_command ;;
    "install kubeseal") gt_install_kubeseal_command ;;
    "install zoxide") gt_install_zoxide_command ;;
    "install eza") gt_install_eza_command ;;
    "install bat") gt_install_bat_command ;;
    "install ripgrep") gt_install_ripgrep_command ;;
    "install starship") gt_install_starship_command ;;
    "install fzf") gt_install_fzf_command ;;
    "install fd") gt_install_fd_command ;;
    "install mcfly") gt_install_mcfly_command ;;
    "install infisical") gt_install_infisical_command ;;
    "install ksops") gt_install_ksops_command ;;
    "install skopeo") gt_install_skopeo_command ;;
    "install helmfile") gt_install_helmfile_command ;;
    "install helm") gt_install_helm_command ;;
    "install chezmoi") gt_install_chezmoi_command ;;
    "install tmux") gt_install_tmux_command ;;
    "install rsync") gt_install_rsync_command ;;
    "install trino") gt_install_trino_command ;;
    "install nerdctl") gt_install_nerdctl_command ;;
    "install viddy") gt_install_viddy_command ;;
    "install hyperfine") gt_install_hyperfine_command ;;
    "install jq") gt_install_jq_command ;;
    "install yq") gt_install_yq_command ;;
    "install gum") gt_install_gum_command ;;
    "install uv") gt_install_uv_command ;;
    "install bashly") gt_install_bashly_command ;;
    "install gem") gt_install_gem_command ;;
    "install claude") gt_install_claude_command ;;
    "install talisman") gt_install_talisman_command ;;
    "uninstall") gt_uninstall_command ;;
    "uninstall kustomize") gt_uninstall_kustomize_command ;;
    "uninstall k9s") gt_uninstall_k9s_command ;;
    "uninstall kubectl") gt_uninstall_kubectl_command ;;
    "uninstall fastfetch") gt_uninstall_fastfetch_command ;;
    "uninstall direnv") gt_uninstall_direnv_command ;;
    "uninstall btop") gt_uninstall_btop_command ;;
    "uninstall zsh") gt_uninstall_zsh_command ;;
    "uninstall antidote") gt_uninstall_antidote_command ;;
    "uninstall thefuck") gt_uninstall_thefuck_command ;;
    "uninstall argocd") gt_uninstall_argocd_command ;;
    "uninstall velero") gt_uninstall_velero_command ;;
    "uninstall kubeconform") gt_uninstall_kubeconform_command ;;
    "uninstall krew") gt_uninstall_krew_command ;;
    "uninstall kubent") gt_uninstall_kubent_command ;;
    "uninstall sops") gt_uninstall_sops_command ;;
    "uninstall age") gt_uninstall_age_command ;;
    "uninstall trivy") gt_uninstall_trivy_command ;;
    "uninstall kubens") gt_uninstall_kubens_command ;;
    "uninstall kubecolor") gt_uninstall_kubecolor_command ;;
    "uninstall popeye") gt_uninstall_popeye_command ;;
    "uninstall kube-linter") gt_uninstall_kube_linter_command ;;
    "uninstall kyverno") gt_uninstall_kyverno_command ;;
    "uninstall conftest") gt_uninstall_conftest_command ;;
    "uninstall vault") gt_uninstall_vault_command ;;
    "uninstall kubeseal") gt_uninstall_kubeseal_command ;;
    "uninstall zoxide") gt_uninstall_zoxide_command ;;
    "uninstall eza") gt_uninstall_eza_command ;;
    "uninstall bat") gt_uninstall_bat_command ;;
    "uninstall ripgrep") gt_uninstall_ripgrep_command ;;
    "uninstall starship") gt_uninstall_starship_command ;;
    "uninstall fzf") gt_uninstall_fzf_command ;;
    "uninstall fd") gt_uninstall_fd_command ;;
    "uninstall mcfly") gt_uninstall_mcfly_command ;;
    "uninstall infisical") gt_uninstall_infisical_command ;;
    "uninstall ksops") gt_uninstall_ksops_command ;;
    "uninstall skopeo") gt_uninstall_skopeo_command ;;
    "uninstall helmfile") gt_uninstall_helmfile_command ;;
    "uninstall helm") gt_uninstall_helm_command ;;
    "uninstall chezmoi") gt_uninstall_chezmoi_command ;;
    "uninstall tmux") gt_uninstall_tmux_command ;;
    "uninstall rsync") gt_uninstall_rsync_command ;;
    "uninstall trino") gt_uninstall_trino_command ;;
    "uninstall nerdctl") gt_uninstall_nerdctl_command ;;
    "uninstall viddy") gt_uninstall_viddy_command ;;
    "uninstall hyperfine") gt_uninstall_hyperfine_command ;;
    "uninstall jq") gt_uninstall_jq_command ;;
    "uninstall yq") gt_uninstall_yq_command ;;
    "uninstall gum") gt_uninstall_gum_command ;;
    "uninstall uv") gt_uninstall_uv_command ;;
    "uninstall bashly") gt_uninstall_bashly_command ;;
    "uninstall gem") gt_uninstall_gem_command ;;
    "uninstall claude") gt_uninstall_claude_command ;;
    "uninstall talisman") gt_uninstall_talisman_command ;;
    "setup") gt_setup_command ;;
    "setup multi") gt_setup_multi_command ;;
    "setup antidote") gt_setup_antidote_command ;;
    "setup starship") gt_setup_starship_command ;;
    "outdated") gt_outdated_command ;;
    "completions") gt_completions_command ;;
  esac
}

initialize
run "$@"
